# 외부 코퍼스 렙 카운터 재생 — 현재 앱 `RepCounter`

반복 정답이 있는 외부 데이터(MM-Fit, REHAB24-6)에 **지금 앱이 세션에서 쓰는 카운터**를 그대로 돌려,
"AIHub 에서 뽑은 렙 로직이 연속 동작·휴대폰 조건에서도 통하는가"를 스쿼트·런지·덤벨 컬 세 종목에서 잰다.

## 0. 상태 (2026-09-24)

| 실험 | 상태 |
|---|---|
| **본 실험 (MM-Fit)** — 영상 → MediaPipe(앱 모델) → 현재 카운터·새 코어 | **실행함(§10).** zenodo.org 가 이 컨테이너에서 막혀 사용자 PC 에서 추출했다(캡처: 브랜치 `data/mmfit-mp-captures`, CC BY 4.0). 설계 `docs/REP_ENGINE_DESIGN.md` §16 |
| **본 실험 (REHAB24-6)** | 판정 기준 사전 등록(설계 §17, `score_rehab_prereg.py`) → 사용자 PC 실행 대기. CC BY-NC 라 파생 캡처는 공개 저장소에 올리지 않고 집계만 |
| **보조 실험** — MM-Fit 데이터셋 자체 3D 포즈 → 현재 카운터 | 실행함(§4). MediaPipe 결과를 대신하지 않는다 |
| **폰 검증 Gate A** — 검증 모드 세션 → 회수 → 채점·프레이밍·무결성 보고서 | PC 파이프라인 준비 완료, 합성 드라이런 통과(§12). 폰 세션 대기 — 절차 `GATE_A_RUNBOOK.md` |

`docs/mmfit-rep-counting.v1.md`·`docs/rehab-rep-f1.v1.md` 의 숫자는 **다른 계열 엔진**(`HeuristicFormCheckSession`,
`app/src/testDebug.other-lineage`)의 측정이다. 지금 앱의 `RepCounter`(복귀형)와는 알고리즘이 다르다.

## 1. 구성

| 파일 | 역할 |
|---|---|
| `replay-jvm/` | 앱 소스 `PostureCore.kt`·`RepCounter.kt`·`ReturnRepTracker.kt`·`RepHysteresis.kt`·`PostureView.kt`(방향 피처) 를 **복사 없이 그 자리에서** 컴파일하는 Kotlin/JVM 재생기. `--dump-features` 로 랜드마크 캡처의 프레임별 앱 피처를 낸다(§12) |
| `extract_mediapipe.py` | 영상 → MediaPipe 캡처 (MM-Fit 세트 / REHAB24-6 녹화) |
| `mmfit_pose3d_captures.py` | MM-Fit `pose_3d.npy` → 같은 형식의 캡처 (보조 실험) |
| `capture_format.py` | 캡처 형식, 앱 추론 주기 |
| `run_replay.py` | 매니페스트 생성 → 재생 → 채점 (세트 반복 수, 반복 경계 F1). 윈도우에서는 재생기 `.bat` 을 부른다 |
| `mmfit_align.py` | MM-Fit 영상 시간축 ↔ 라벨 정렬 — 세트별 지연(pose_3d 교차상관)으로 정답 창을 영상 시각으로 옮긴 index (§10) |
| `score_rehab_prereg.py` | REHAB24-6 사전 등록 판정(설계 §17) — 발화 시각 경계 일치, 카메라별, 정자세/비정자세 재현율, P1~P4 |
| `prototype_counter.py` · `stress_battery.py` · `parity_core.py` | 새 코어 프로토타입(§8) · 시작 확정 정책 배터리(설계 §12·§13) · Kotlin 새 코어 ↔ 프로토타입 세트별 파리티 |
| §9 의 도구들 | 휴대폰 세트 로그 재생·채점·검증 프로토콜(휴대폰 연결 전 준비분) |
| §12 의 도구들 | 폰 검증 Gate A 한 번에 돌리기 — `pull_phone.py`(adb 회수·검증 모드 스위치) · `gate_a.py`(보고서) · `make_phone_fixture.py`(폰 없는 드라이런 픽스처), 절차 `GATE_A_RUNBOOK.md` |
| `results/` | 요약 수치 (원본 캡처는 `data/` — git 제외) |

## 2. 앱과 맞춘 것, 다른 것

**맞춘 것**
- 카운터 구성: `PostureLive` 의 서서 하는 종목 경로 그대로 — `RepCounter(signal, maxGapMs = 1500, completeOnReturn = true)`.
  `rules_mp_v0.json` 에 `kind=rep` 규칙이 없어 신호·ROM 기준은 `RepSignals` 등록부 값이다.
- 신호: 스쿼트 `knee_mean`(35°), 런지(앱 '런지' = 스텝 포워드 다이나믹 런지) `knee_out_mean`(0.10), 덤벨 컬 `elbow_mean`(35°).
  (§4·§7 을 잰 2026-09-23 시점의 앱 신호다. 2026-09-24 에 앱 런지 신호를 `knee_mean`(35°)으로 바꿨다 — 설계 §4.1, spec §57. 지금 `live` 구성의 런지는
  `knee_mean` 이고 옛 신호는 진단 구성 `live+knee_out_mean` 으로 남았다, §9.)
- 추론 후처리: `PostureAnalyzer.analyzeBitmap` 과 같은 순서(visibility·presence 최솟값, 0.5 미만 관절 제외,
  m→cm·y·z 반전, `checkUpSanity`, `PoseFrame.features()`). 이 부분만 Android 타입 때문에 절차를 옮겨 적었다(`Replay.kt` 머리 주석).
- MediaPipe: 앱 번들 모델(`posture/pose_landmarker_full.task`, SHA `4eaa5eb7…` — 다른 계열 측정과 같은 파일),
  VIDEO 모드, `num_poses=1`, 신뢰도 0.5, `mediapipe==0.10.14`(앱 tasks-vision 과 같은 버전).
- 추론 주기: 직전 추론 이후 300ms 이상인 프레임만. 건너뛴 프레임은 랜드마커에도 넣지 않는다.
- 사람이 검출된 프레임에서만 `onFrame` 을 부른다(앱과 같다).

**다른 것 — 결과를 읽을 때 따라가야 한다**
- 카메라: 휴대폰이 아니다. MM-Fit 은 고정 정면 한 대(Orbbec). REHAB24-6 은 같은 자리의 두 대로 가로(cam17)·세로(cam18) 화면비만 다르다
  (`docs/rehab-rep-f1.v1.md` §3.4) — 같은 반복을 다른 **각도**에서 본 자료가 아니다.
- 중력: 영상에 IMU 가 없어 up = 화면 세로축(앱의 센서 없는 폴백).
- 델리게이트 CPU(앱은 GPU 우선), 발열 감속 없음.
- 30fps 영상 시각이 정수 ms 라 추론 간격이 정확히 300ms 다(실기기는 지터로 300~333ms).

## 3. 검증

- 앱 저장소의 카운터 유닛 테스트 19개(`RepCounterTest` 12, `ReturnRepTrackerTest` 7)를 이 재생기가 컴파일한
  카운터로 돌려 전부 통과했다 — 재생기의 카운터가 앱 테스트가 기대하는 그 카운터다.
- MediaPipe 공개 테스트 이미지(`mediapipe-assets/pose.jpg`)로 만든 합성 영상으로 추출 → 재생 → 채점을 끝까지 돌렸다
  (11샘플 전부 검출, 피처 정상). **실제 반복 영상으로는 아직 검증하지 않았다.**
- 경계 채점 함수는 손으로 계산한 예제와 일치한다.

## 4. 보조 실험 결과 — MM-Fit 자체 3D 포즈 (MediaPipe 아님)

데이터: MM-Fit (Strömbäck, Huang, Radu, IMWUT 2020, doi:10.1145/3432701, **CC BY 4.0**) 21개 워크아웃,
세 종목 185세트(스쿼트 64, 런지 62, 컬 59). 포즈는 데이터셋이 제공한 H36M 17관절 3D(연속 30fps 영상에서 추정).
세트 정답은 반복 수뿐이라 경계 F1 은 계산할 수 없다. 앞뒤 여유는 다른 계열과 같은 0.5초, 민감도로 2초.

**앱 구성(live)** — 과다 카운트는 모든 구성에서 0건(정밀도 1.000)

| 종목 · 신호 | 세트 | 정답 | 검출 | ROM 유효 | MAE | OBO | 정확 일치 | 0회 세트 | 재현율 | count F1 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 스쿼트 `knee_mean` (여유 0.5s) | 64 | 639 | 576 | 161 | 0.98 | 0.81 | 0.53 | 1 | 0.901 | 0.948 |
| 스쿼트 (여유 2s) | 64 | 639 | 604 | 171 | 0.55 | 0.88 | 0.75 | 1 | 0.945 | 0.972 |
| 런지 `knee_out_mean` (0.5s) | 62 | 624 | 62 | 62 | 9.06 | 0.00 | 0.00 | 39 | 0.099 | 0.181 |
| 런지 (2s) | 62 | 624 | 62 | 62 | 9.06 | 0.00 | 0.00 | 41 | 0.099 | 0.181 |
| 덤벨 컬 `elbow_mean` (0.5s) | 59 | 599 | 512 | 14 | 1.47 | 0.64 | 0.42 | 0 | 0.855 | 0.922 |
| 덤벨 컬 (2s) | 59 | 599 | 489 | 14 | 1.86 | 0.63 | 0.49 | 2 | 0.816 | 0.899 |

**진단 — 앱 동작이 아니다.** 같은 복귀형 구성에 신호만 바꿨다. 앱의 다른 종목이 이미 쓰는 신호만 골랐고 ROM 기준은 뗐다.

| 종목 · 신호 | 여유 0.5s 재현율 / F1 | 여유 2s 재현율 / F1 |
|---|---|---|
| 런지 `knee_mean` (앱 '크로스 런지' 신호) | 0.838 / 0.912 | 0.875 / 0.933 |
| 런지 `knee_minside` (앱 '바벨 런지' 신호) | 0.689 / 0.816 | 0.766 / 0.868 |
| 덤벨 컬 `elbow_minside` | 0.873 / 0.932 | 0.851 / 0.920 |
| 반전형(`RepCounter` 기본 구성, 앱 세션은 안 씀) — 스쿼트·런지·컬 재현율 | 0.886 · 0.077 · 0.751, 정확 일치 0 | 0.887 · 0.077 · 0.755, 정확 일치 0~0.02 |

### 해석

1. **런지 — 신호 선택 자체가 연속 동작에서 무너진다.** `knee_out_mean` 은 62세트 중 39세트를 0회로 셌다.
   세트별 흔들림(p95−p5) 중앙값이 0.090 으로 최소 스윙 0.10 에 못 미친다. 앞으로 딛는 런지에서 무릎은 앞으로 나가지
   옆으로 벌어지지 않으므로, 옆 이탈 신호에는 반복이 담기지 않는다. 이 신호는 AIHub 0.6초 간격 조사(`REP_SIGNALS.md`)에서
   일관성 1등이었다 — 성긴 데이터에서 고른 신호가 조밀한 연속 데이터에서 통하지 않은 직접 사례다. MediaPipe 이전의 문제다.
2. **스쿼트 — 세기는 대체로 된다. 놓치는 원인은 두 가지다.**
   - 기준 자세: 복귀형 추적기는 한 자세에 3샘플 연속(300ms 간격이라 최소 600ms) ±7.7° 안에 머물러야 기준을 잡는다.
     쉬지 않고 연속으로 하면 세트 전체가 0회다. w00 첫 세트는 무릎각이 70°↔165° 로 10번 뚜렷한데 0회였다.
     여유를 2초로 늘리면(준비 자세가 보이면) 재현율 0.901 → 0.945.
   - 템포: 반복당 1.6초 미만 세트는 재현율 0.50(3세트), 1.6~2.0초 0.88(34세트), 2초 이상 0.97(27세트).
   - ROM: 검출된 반복의 28%(161/576)만 유효다. 코치 모드였다면 대부분을 "무효"로 말했다. 원인은 둘로 갈린다.
     (a) 정의 불일치 — 카운터는 300ms 간격이면 3점 중앙값으로 평활하고, ROM 은 **평활된** 사이클 최저값을 본다. 바닥은 보통
     한 샘플에만 찍히므로 평활이 그것을 지운다(세트 하위 2% 무릎각: 원값 92.3° → 평활 101.1°). 기준 97.9° 는 평활하지 않은
     AIHub 값에서 뽑았다. 반복 사이 구간의 원값 최저를 쓰면 유효가 346/576(60%)으로 는다(근사 추정).
     (b) 동작 차이 — 나머지는 MM-Fit 맨몸 스쿼트가 AIHub 기준보다 얕아서다. 이 3D 포즈의 무릎각이 MediaPipe 와 다를 수 있어
     두 몫의 크기는 본 실험으로 다시 봐야 한다.
3. **덤벨 컬 — 교대 동작에서 세기는 되지만 ROM 이 거의 전부 무효다.** MM-Fit 컬은 좌우 교대다. 한쪽 팔을 굽힐 때마다
   양팔 평균이 약 50° 움직여 35° 게이트를 넘으므로 센다(재현율 0.82~0.86, 1.6초 미만 템포 7세트는 0.59).
   그러나 평균은 81.3°(ROM 기준) 아래로 내려갈 수 없어 유효 판정이 14/512(3%)다.
   여유를 2초로 늘리자 2세트(w00·w05 마지막 컬 세트)가 0회로 바뀌었다. 복귀형 추적기는 **처음 안정된 자세**(3샘플 ±7.7°)를
   세트 내내 기준으로 고정하는데, 이 두 세트는 덤벨을 든 채 팔꿈치가 122~129° 로 굽은 준비 구간에서 기준을 잡았다.
   이후 컬은 그 기준에서 35° 이상 벗어나지 못해 한 번도 "움직임"으로 시작되지 않았다.
4. **반전형은 정확 일치가 0이다** — 다음 하강이 있어야 상단을 확정하므로 세트의 마지막 반복을 구조적으로 놓친다(`CLAUDE.md` 함정과 같다).
   앱 세션은 복귀형을 쓰므로 이 결함은 세션 카운트에는 해당하지 않는다.

가설과 연결하면: 카운트 **메커니즘**(복귀형)은 깨끗한 연속 골격에서 대체로 작동한다. 반면 AIHub 에서 온 **선택과 기준**
(런지 신호, ROM 임계값)은 연속 데이터에서 무너졌다. 휴대폰·MediaPipe 조건이 여기에 무엇을 더하는지는 본 실험이 답한다.

## 5. 한계

- 보조 실험의 포즈는 MediaPipe 가 아니다(2D→3D 추정). 여기서 잘 센다고 휴대폰에서 잘 센다는 뜻이 아니다.
- MM-Fit: 고정 정면 카메라, 맨몸 스쿼트·런지, 세트 단위 정답(경계 없음). 여유 길이에 따라 결과가 달라진다(§4 두 줄).
- 이 결과로 임계값·신호를 적합하지 않았다. REHAB24-6 은 CC BY-NC 4.0 이라 측정에만 쓸 수 있다.

## 6. 실행법

```bash
# 0) 의존성 — Python 3.11, JDK 17+, gradle 8.x
python -m venv .venv-mp && .venv-mp/bin/pip install "mediapipe==0.10.14" opencv-python-headless numpy remotezip
(cd replay-jvm && gradle test installDist)      # 앱 카운터 유닛 테스트 19개도 여기서 돈다

# 1) MM-Fit 라벨·3D 포즈 (mm-fit.zip 에서 필요한 파일만): 보조 실험
python mmfit_pose3d_captures.py ../../data/mm-fit/mm-fit --out ../../data/mm-fit/captures_pose3d
python run_replay.py ../../data/mm-fit/captures_pose3d --out ../../data/mm-fit/results_pose3d

# 2) 본 실험 — 영상을 받아서 (이 컨테이너는 zenodo.org 가 막혀 사용자 PC 에서 돌렸다 — §10. 윈도우는 PYTHONUTF8=1)
python extract_mediapipe.py mmfit ../../data/mm-fit/mm-fit --videos ../../data/mm-fit/videos --out ../../data/mm-fit/captures_mp
python run_replay.py ../../data/mm-fit/captures_mp --out ../../data/mm-fit/results_mp
python extract_mediapipe.py rehab ../../data/rehab24-6 --videos ../../data/rehab24-6/videos --out ../../data/rehab24-6/captures_mp
python run_replay.py ../../data/rehab24-6/captures_mp --out ../../data/rehab24-6/results_mp   # 경계 F1 포함
```

## 7. "촬영기기·각도 때문에 MM-Fit 에서 안 된다" 가설 검토 (2026-09-23)

주장: 현재 엔진은 AIHub 로 배운 것이고, MM-Fit 에서 제대로 안 되는 이유는 촬영기기·각도이므로, MM-Fit 도 정답 기준으로 삼는다.
실험 4개(시점 변환, 실제 코드 상태 계측, 문서 감사, MM-Fit 라벨 감사)와 찬반 논증, 독립 판정 2개로 검토했다.
작업 산출물은 `data/mm-fit/exp_*`(git 제외).

| 고리 | 판정 | 결정적 근거 |
|---|---|---|
| 엔진은 AIHub 로 배웠다 | 절반만 맞다 | AIHub 에는 반복 정답이 없어 횟수를 학습한 부품이 없다. AIHub 에서 온 것은 런지 신호 선택과 ROM 기준뿐이고, 복귀형 추적기·35° 게이트·평활은 설계와 폰 데이터에서 왔다(spec §27·§42, `RepCounter.kt` 주석) |
| MM-Fit 에서 안 된다 | 맞다 (대리 입력 기준) | 세트 정확 일치 스쿼트 0.53 · 런지 0.00 · 컬 0.42. 185세트 중 174세트가 10회라 "항상 10" 이 엔진보다 낫다(MAE 0.08·0.10·0.15 대 0.98·9.06·1.47) |
| 원인은 촬영기기·각도다 | 반증 (측정된 실패에 대해) | ① 폰·MediaPipe 가 없는 입력에서 같은 실패가 난다 ② 같은 300ms 신호에 평활·기준자세 없는 단순 인과 카운터(35°)를 돌리면 스쿼트 0.997 · 런지(`knee_mean`) 0.990 · 컬 0.998 — 반복은 신호 안에 있고 엔진이 잃는다 ③ 스쿼트·컬 신호는 세 관절 각이라 회전 불변: 시점 변환 26개에서 123세트 중 0세트 변화 ④ 놓친 712회 중 기기·각도에 민감한 원인은 최대 4회 ⑤ 스쿼트는 MM-Fit 정면이 앱 권장 방향과 같다 |
| 그래서 MM-Fit 도 정답으로 | 결론만 맞다, 이유는 틀렸다 | 라벨은 실제 센 값이다(3D 포즈 오프라인 계수와 180/185 세트 일치). 이유는 각도가 아니라 "AIHub 가 줄 수 없는 연속 세트 횟수 정답"이라서다. 원인이 정말 기기·각도라면 필요한 기준은 앱 권장 방향의 폰 데이터이지 MM-Fit 이 아니다 |

놓친 반복의 원인(실제 코드의 내부 상태를 매 프레임 읽어 배정): 런지는 신호 폭 부족(562회 중 513회, `knee_out_mean` 반복당 이동 중앙값
0.052 < 게이트 0.10, 두 반복마다 되풀이되는 보폭 신호). 스쿼트·컬은 상단 체류 부족으로 복귀 조건 미충족(26·29), 3점 중앙값 평활(7·40),
0.5s 여유에서 마지막 반복 잘림(20·14 — 평가 인공물), 준비 자세에 잘못 잡힌 기준. 불응기 1.2s 와 1.5s 끊김 초기화는 영향 0.
시점 변환 중 결과를 움직인 것은 IMU 가 없을 때의 카메라 기울기가 런지 `knee_out_mean` 에 들어가는 경로 하나다(재현율 0.06~0.35).
앱은 중력 센서가 있으면 이 경로를 닫고, 최선의 경우도 `knee_mean` 0.84 에 못 미친다.

**MM-Fit 을 쓰는 규칙.** 세트 단위 반복 수와 휴식 중 오카운트(현재 컬 1.53회/분)의 정답으로만 쓴다. 참여자 10명 중 P0·P1 이 반복의 56% 라
피험자 단위로 나눠 보고한다(처음 보는 피험자 재현율: 스쿼트 0.833, 컬 0.729). "항상 10" 기준선을 같은 표에 싣는다.
ROM·깊이 기준은 맞추지 않는다 — 스쿼트 바닥 중앙값이 사람마다 64.9°~108.2° 인 스타일 차이라 원칙 #3·#4 에 걸린다.
교대 컬은 양팔 평균 81.33° 에 구조적으로 닿지 못한다(한 팔 기준 601/604 통과) — 기준값이 아니라 판정 정의의 문제다.

**아직 답이 없는 것.** MediaPipe 가 시점에 따라 더하는 추정 오차(특히 정면에서 카메라 쪽으로 굽는 팔꿈치), 폰 조건(IMU·GPU·지터).
MM-Fit 영상으로 같은 185세트를 재생해 대리 입력과 세트별로 짝지으면 MediaPipe 의 몫이 나온다. 각도 자체는 같은 반복을 두 방향에서
찍은 자료로만 판정된다(MM-Fit 두 번째 시점 영상이 있으면 그것, 없으면 폰 두 대 동시 촬영).

## 8. 카운터 코어 프로토타입 (설계 검증)

`prototype_counter.py` 는 `docs/REP_ENGINE_DESIGN.md` §4.2~4.4 의 카운터(복귀 히스테리시스 + 시작 확정 + 교대 병합)를 파이썬으로 구현해
MM-Fit 자체 3D 포즈에서 세트 카운트·휴식 헛카운트·세트 직전 10초 헛카운트를 함께 잰다. 앱 코드가 아니고, 상수는 모집단 값이다.
결과 표는 설계 문서 §5. 현재 엔진의 세트 직전 헛카운트는 같은 창을 캡처로 만들어 JVM 재생기로 쟀다(스쿼트 0 · 런지 0 · 컬 4/59창).

`noise_stress.py` 는 같은 프로토타입과 현재 엔진(JVM)에 관절 지터·한 프레임 튐·검출 끊김·추론 간격 지터·발열 감속·깊은 굴곡 편향을 단계별로 넣어
어느 크기에서 무엇이 먼저 무너지는지 잰다(설계 문서 §9). 폰 잡음의 실측 분포가 없으므로 학습 데이터가 아니라 안전 여유의 측정이다.

## 9. 휴대폰 없이 준비한 배관 (2026-09-24)

휴대폰을 연결할 수 없는 동안 만든 도구다. 전부 합성 데이터나 기존 MM-Fit 3D 로 한 번 이상 돌렸고, 결과 파일은 `data/mm-fit/exp_plumbing/`(git 제외)에 있다.

| 파일 | 역할 | 검증 |
|---|---|---|
| `run_replay.py` (개정) | `captureStartMs/EndMs` 가 있는 index 면 세트 창(라벨 ± 0.5 s) 밖 발화를 앞·뒤 헛카운트로 가른다. 구성 `hysteresis`(새 코어)·신호 후보, 항상-N 기준선, 사람별 행, 사람 ≥ 5명이면 군집 부트스트랩 95% 구간, 세트 로그 파리티 표(로그의 `reps.engine` 이 가리키는 구성에서만) | 0.5 s 캡처에서 README §4·코어 포팅 수치를 그대로 재현 |
| `setlog_captures.py` | 휴대폰 세트 로그(`sets-*.jsonl`) → 피처 캡처 `*.fcap` + index. 자가 라벨 edited/confirmed 를 나눈다. spec §58 필드(`reps.engine`·`config`·`resets`(누른 시각 + `after_t_ms`)·`pending`, `thermal`, `app_version`)를 넘긴다. **표시 단위**(spec §59, 런지류 좌우 한 쌍 = 1회): `reps.unit`·`cycles_per_rep`·`completed`·`half_pending` → `repUnit`·`loggedDisplayedReps`·`loggedHalfPending`·`loggedUnitConsistent`(없으면 옛 로그 = 사이클), 지금 앱 단위 `currentUnit`(`SIDE_PAIR_AIHUB`, 코틀린과 대조). 라벨은 화면 단위라 둘 다 적는다 — `truthDisplayedReps`(라벨 그대로)와 사이클 범위 `truthCyclesMin`~`truthCyclesMax`(라벨 × 사이클/회 ~ +1, 짝 없이 끝난 한쪽). `truthReps`(run_replay·parity_core 가 정확한 정답으로 쓰는 사이클 수)는 범위가 한 값일 때만 채우고 쌍 라벨이면 None — 짝 없는 한쪽으로 끝난 정직한 세트를 과다로 채점하지 않게. `set_labels.jsonl` 의 `rep_unit`(앱이 라벨을 적을 때의 화면 단위) → `labelUnit`·`labelUnitMatchesLog` | `--self-test` 46/46 — §12 이후 49/49 (손으로 쓴 로그 왕복, 옛 로그, 리셋 재생, 추론 중에 누른 전환의 `after_t_ms` 순서(H/H2), 새 코어 로그의 버림·대기, 실기기 baseline1 = 현재 카운터 0 · 새 코어 4, **코틀린 `SetLogJson` 골든 줄**(`app/src/test/resources/setlog_s58_fixture.txt`, 좌우 짝 줄 포함)과 파이썬 인코더 사본의 바이트 일치·변환, 좌우 짝: 홀수 5걸음 로그 → 화면 2 + 반쪽, edited/confirmed 2쌍 → 사이클 4~5(사이클 정답 없음), run_replay 채점에서 쌍 라벨 세트가 과다로 잡히지 않음, 라벨 jsonl 의 화면 단위 대조, 사이클끼리 파리티, completed 불일치 음성 대조, unit 없는 옛 런지 로그, 코틀린 종목 표 대조) |
| `replay-jvm` `Replay.kt` (개정) | `*.fcap` 읽기(`readFeatureCapture`), 매니페스트 7~10열(floor·규칙 ROM·새 코어 극성), 로그의 리셋을 앱과 같은 순서(`after_t_ms` 가 있으면 그보다 늦은 첫 프레임 앞)로 `resetCycle()`, 파리티 필드(카운트·시각·ROM·버림·대기), 연구용 파생 신호 `*_minside_both`, 값 프레임 구간 `valueSpans`. hysteresis 구성은 극성을 로그 → 등록부 → 연구 표(세 대상 종목 + 푸시업류) 순서로 정하고 모르면 재생하지 않는다 | `gradle test` 41/41 — §12 이후 46/46 |
| `parity_core.py` | Kotlin 새 코어(재생기 hysteresis) ↔ 프로토타입(`prototype_counter.py` + first8+amp) 세트별 파리티 — 같은 입력(재생기 신호값)에서 카운트·발화 시각·확정 대기, 프로토타입 피처에서 카운트 | MM-Fit 3D 244/244 (불일치 0, 종료 코드로 알린다) |
| `mmfit_pair_compare.py` | MediaPipe 캡처 ↔ pose_3d 짝짓기: 지연(교차상관)·좌우·각도 오차(깊이별)·휴식 지터·튐·끊김·같은 시각 3D 대비 카운트 차이. `synth` 는 도구 검증용 합성 캡처 | `selftest` 17/17 (지연 4 복원, 잡음 없으면 카운트 차이 0/54, 주입 끊김·먼 팔 가림·지터·튐을 되찾음) |
| `phone_noise.py` | 캡처·세트 로그에서 추론 간격(발열 대리)·추론 지연·로그의 열 상태별 간격·휴식 σ·튐·끊김 | 합성·세트 로그·피처 캡처에서 실행 |
| `score_phone_reps.py` | 계획표와 로그 짝짓기, 정답 등급(집계표 / edited / confirmed=순환), Clopper-Pearson 단측 경계, 항상-계획 기준선, 정지 표지 + 오프라인 사이클로 앞·뒤 헛카운트(경계 게이트는 로그가 센 신호의 게이트 — 옛 런지 로그는 `knee_out_mean` 0.10), Gate B 판정. **런지류는 두 단위**로 채점한다 — 걸음(사이클, 정답 = 집계 왼 + 오른)과 쌍(화면 수·자동 진행, 정답 = min(왼, 오른), 예측 = 사이클 // 2 · 로그 `completed`), 쌍 표에 위상 밀림(세트 앞 헛사이클 홀수)·반사실 조기 자동 진행, Gate B 는 두 단위 모두 통과해야 통과. 쌍 단위 자가 라벨은 걸음 정답에 넣지 않는다. 팔별 집계가 있는 컬 세트(Gate A `altcurl`, 번갈아 하는 컬)는 쌍 표에 참고로 싣고(예측 = 사이클 그대로 — 앱은 컬을 짝짓지 않는다) 판정에서 뺀다 | `--self-test` 42/42 (설계 §7 세트 수 29·46·61·76·149·59·93 재현, 경계 게이트 출처, 파일럿 런지 쌍 행, 좌우 짝 시나리오 10세트: 짧은 세트의 반쪽, 왼 6·오른 4, 준비 굽힘 1번의 위상 밀림·조기 진행, 쌍 라벨·옛 집계·옛 빌드 라벨의 단위, 순서 검사, 두 단위 판정, 번갈아 하는 컬: 계획의 블록 끝 altcurl · 걸음 정답 = 팔 반복 합 · 쌍 정답 = min · 판정 밖) |
| `rep_validation_plan.py` → `REP_VALIDATION.md` | 파일럿 · Gate A · Gate B 수집 프로토콜과 무작위 계획표 CSV. 계획 수는 카운터 사이클(런지 = 걸음, 화면 수 `planned_reps` = 그 절반). 런지 집계는 `tally_left`·`tally_right`(+ 영상이면 `tally_order`), Gate A 에 런지 한쪽 몰아 하기(`sideblock`) 1세트, 컬의 판정 세트는 양팔 동시이고 번갈아 하는 컬은 Gate A `altcurl` 1세트(팔별 `tally_left`·`tally_right`, 판정 밖 — 설계 §15 #25) | 생성·구조 확인, 계획 CSV 는 `score_phone_reps.py --self-test` 가 읽는다 |

바뀐 동작: 매니페스트는 캡처 폴더가 아니라 `--out` 폴더에 쓰고 캡처 경로를 절대경로로 적는다. 세트 앞뒤 헛카운트의 **분당** 비율은 창 길이가 아니라
카운터가 값을 실제로 본 시간(`valueSpans`, 1.5 s 넘는 틈으로 끊은 구간)으로 나눈다 — 사람이 안 잡힌 프레임은 헛카운트를 낼 수 없다. 세트당 비율은 그대로다.
hysteresis 행은 극성을 아는 종목에만 만든다(`HYSTERESIS_POLARITY`). 재생 결과의 `valid` 는 ROM 판정이 참인 반복만이다 —
ROM 기준이 없는 신호(지금의 런지 `knee_mean`)는 `romUnjudged` 로 가고 표에는 '유효/미달/미판정' 으로 나온다(설계 §4.5). 런지 `live` 는 이제 `knee_mean` 이라
§4 표의 런지 행(`knee_out_mean`)은 진단 구성 `live+knee_out_mean` 으로 남았다.

**MM-Fit 3D 에 세트 앞 15 s · 뒤 10 s 를 이어 붙인 재생** (`mmfit_pair_compare.py synth --lag 0` 에 잡음 0 → `run_replay.py`, 개발 데이터, MediaPipe 아님).
세트 창 정확 일치 / 앞 헛카운트·세트 / 뒤 헛카운트·세트 / 앞뒤까지 이어 센 전체의 정확 일치:

| 종목 | 현재 엔진 (live) | 새 코어 (hysteresis) | 항상 10 |
|---|---|---|---|
| 스쿼트 `knee_mean` | 0.50 / 0.00 / 0.31 / 0.64 | 0.97 / 0.00 / 0.09 / 0.92 | 0.95 |
| 런지 `knee_mean` | 0.44 / 0.11 / 0.34 / 0.45 | 0.94 / 0.21 / 0.11 / 0.74 | 0.95 |
| 컬 `elbow_mean` | 0.15 (재현율 0.467) / 0.24 / 0.36 / 0.14 | 0.95 / 0.25 / 0.36 / 0.53 | 0.92 |

- 현재 엔진은 앞 창이 붙으면 컬 재현율이 0.855 → 0.467 로 무너진다 — 세트 전 휴식 자세를 기준으로 고정한 채 세트를 맞는다(§4 해석 3과 같은 원인).
  앱에서는 2세트부터 준비 카운트다운 없이 카운터가 돌므로(설계 §14) 같은 노출이 있다.
- 새 코어는 세트 안은 지키지만(0.94~0.97), 앞뒤 움직임까지 센 전체는 런지 0.74 · 컬 0.53 으로 항상-10 아래다. 세트 경계가 가장 큰 위험이라는 설계 §14 와 같은 방향이다.
  MM-Fit 의 앞뒤 창은 세트 사이 휴식이지 카운트다운 직후가 아니므로 크기는 폰(Gate A)에서 다시 잰다.
- 앞 창이 붙으면 새 코어도 세트 안 과다 카운트가 생긴다(런지 2세트 · 컬 1세트, 0.5 s 캡처에서는 0).

## 10. 본 실험 — MM-Fit 영상 → MediaPipe (2026-09-24)

사용자 PC 가 MM-Fit 21개 워크아웃 영상(Zenodo 7672767)을 한 개씩 받아 `extract_mediapipe.py mmfit --pre-s 15 --post-s 10` 으로 뽑았다
(앱 모델 SHA `4eaa5eb7…`, VIDEO, 300 ms, 긴 변 640, CPU 델리게이트 — 앱은 GPU 우선). 185세트(스쿼트 64 · 런지 62 · 컬 59), 캡처는 브랜치 `data/mmfit-mp-captures`.

```bash
git show origin/data/mmfit-mp-captures:mmfit_mp_captures.zip > ../../data/mm-fit/mp_captures.zip && unzip -q ../../data/mm-fit/mp_captures.zip -d ../../data/mm-fit/mp_captures
python mmfit_align.py ../../data/mm-fit/mp_captures ../../data/mm-fit/mm-fit --out ../../data/mm-fit/mp_captures_aligned
python run_replay.py ../../data/mm-fit/mp_captures_aligned --out <결과> \
    --configs live,live+knee_out_mean,live+elbow_minside,hysteresis,hysteresis+knee_minside,hysteresis+elbow_minside
python mmfit_pair_compare.py compare <w00~w15 만 담은 캡처 폴더> ../../data/mm-fit/mm-fit --out <결과>
```

- **w16~w20 은 영상이 라벨보다 점점 앞선다**(초당 약 −0.05프레임, 후반 컬 세트에서 −2.5~−5.8 s; w19 는 +30프레임 도약). w00~w15 는 지연 0.
  `mmfit_align.py` 가 세트마다 지연을 재 정답 창만 옮긴다 — 카운터는 그대로다. 짝짓기(`mmfit_pair_compare.py`)는 워크아웃당 지연 하나를 가정하므로 w00~w15 로만 돌렸다.
- 결과: `results/mmfit_mp/replay_aligned.md`(판정 표) · `replay_raw.md`(보정 전) · `pair_w00_15.md`(3D 짝짓기) · `alignment.json`(세트별 지연) ·
  `replay_aligned.summary.json`(세트별 행을 뺀 요약). 해석은 설계 §16.

| 세트 정확 일치 (재현율) | 스쿼트 | 런지 | 컬 |
|---|---|---|---|
| 항상 10 | 0.95 | 0.95 | 0.92 |
| 지금 앱 `live` | 0.33 (0.73) | 0.18 (0.70) — 교체 전 `knee_out_mean` 0.00 (0.08) | 0.00 (0.09) |
| 새 코어 `hysteresis` | 1.00 (1.00) | 0.97 (1.00) | `elbow_mean` 0.00 (0.14) · `elbow_minside` 0.85 (0.98) |

같은 샘플 시각의 3D 와 비교하면(w00~w15) MediaPipe 는 새 코어의 스쿼트·런지 카운트를 세트당 ±0.04회밖에 바꾸지 않는다. MediaPipe 는 무릎을 약 9° 더 굽게,
깊이 굽힌 팔꿈치를 24~29° 얕게 읽고, 휴식 지터 σ 는 2.0~2.8°(3D 0.9°)다. 세트 앞 헛카운트는 런지에서 3D 의 약 2배다(0.17 → 0.36/세트).

## 11. 좌우 한 쌍 = 1회 — 타당성 조사 (2026-09-24)

사용자 결정: 교대 동작은 **좌우 한 번씩 = 1회**(목표 10회 = 왼 10 + 오른 10), 런지 자동 진행은 유지. `side_attribution.py` 가 이 정의를 MM-Fit 에서 잰다
(개발 데이터 — 타당성이지 성능 주장이 아니다. 정면 고정 카메라 한 대이고 앱의 런지 권장 방향 B 대각은 없다). 표 전부: `results/side_attribution/summary.md`,
원자료(사이클·걸음별 좌우, 합성 캡처·재생): `data/mm-fit/exp_sidepair/`(git 제외). `--self-test` 32/32(합성 자료, 데이터·JVM 불필요).

```bash
python side_attribution.py --self-test
python side_attribution.py            # 기존 정렬 재생(exp_mp/aligned) + 3D 재생·레인·합성을 다시 돌린다(약 20 s)
```

**Q1 런지 쌍 표시 = floor(세트 창 안 사이클/2), 정답 쌍 = floor(정답/2).** 홀수 정답 2세트(9·11)는 3D 기준에서도 교대라(RLRLRLRLR) floor 와 min(L, R) 이 같다.

| 세트 정확 (±1) | MediaPipe `live` | MediaPipe `hysteresis` | 3D `live` | 3D `hysteresis` |
|---|---|---|---|---|
| 걸음 단위 (지금) | 0.18 (0.40) | 0.97 (1.00) | 0.48 (0.68) | 0.97 (0.97) |
| 쌍 단위 | 0.19 (0.61) | 0.98 (1.00) | 0.48 (0.77) | 0.97 (0.98) |
| 앞 15 s · 뒤 10 s 까지 센 전체: 걸음 → 쌍 (과다 세트) | 0.19 → 0.31 (10 → 3) | 0.56 → 0.79 (27 → 13) | — | — |

항상 5쌍은 0.97 이다(62세트 중 60세트가 5쌍). 쌍 단위의 이득은 floor 가 걸음 하나의 오차를 **숨기는** 것이지 카운터가 나아진 것이 아니다 — 세트 창 안에서는
새 코어 +1 한 세트·레거시 −1 한 세트(요약 표 '쌍은 맞고 걸음은 틀린 세트'), 앞뒤 창까지 세면 주로 세트 앞 헛사이클의 과다다.
**자동 진행 시점은 두 단위에서 모든 세트가 같다** — 목표 P 쌍은 사이클 2P 에서 닿으므로 걸음 단위 목표 2P 와 같은 순간이다. 목표가 해당 걸음 바닥보다 먼저
닿은 세트는 새 코어 18/61·레거시 8/21(닿은 세트 중)이고, 그중 세트 앞 헛사이클이 있는 세트가 새 코어 17/18·레거시 8/8 이다. 게다가 세트 앞 헛사이클이 **홀수**인 세트(새 코어 16/62)는
쌍의 위상이 한 걸음 밀린다 — 세트 내내 '반대쪽 차례' 가 실제로 한 쌍을 끝낸 순간에 뜨고 마지막 쌍의 둘째 걸음 전에 자동 진행된다. MM-Fit 의 세트 앞 창은
세트 사이 휴식이지 앱의 카운트다운 직후가 아니므로 크기는 Gate A 에서 다시 잰다(설계 §14).

**Q2 걸음 좌우 판별 — 규칙:** 걸음 바닥(knee_mean 최저 + 15°)에서 몸 전방 F = (엉덩이 선의 수평 성분) × up 쪽으로 앞에 있는 발이 그 걸음의 다리.
참 좌우 정답은 없다 — 3D 기준(pose_3d)도 단안 추정이라 교대율과 단서 일치로 점검했다.

- 3D 기준: w00~w15 는 47/47 세트가 **완전 교대**(43 세트가 오른발부터), 굽힌 무릎이 F 쪽 1.000(좌표계·좌우 이름 가정 확인). w16~w20 은 7/15 세트만 교대이고
  같은 세트를 MediaPipe 는 14/15 교대로 본다 — 이 묶음에서는 3D 기준이 틀린다(같은 다리 연속 RRRRRRLRRR 같은 줄). 3D 의 '뒷무릎이 낮다' 단서는 앞발 규칙과 0.84 만 맞는다.
- MediaPipe 월드 좌표에 같은 규칙(위 = 화면 세로축): 3D 기준 걸음에서 w00~w15 일치 **0.983**(471걸음), 카운터(새 코어) 사이클에서 0.972. 틀린 8걸음은
  전부 기준 R → MediaPipe L 이고 **세 단서(앞발·뒷무릎 높이·2D 앞발)가 함께 뒤집혔다** — 기하가 약해서가 아니라 MediaPipe 가 두 다리 이름을 통째로 바꾼 것이다.
  그래서 확신 여유로 걸러지지 않는다 — 새 코어 사이클(w00~w15)의 틀린 13건 중 10건이 가장 큰 여유(20 cm) 밖이다(틀림 |d| 중앙값 45.9 cm, 맞음 59.3 cm).
  13건 중 9건이 한 사람(P1: w02 4 · w04 3 · w07 2)의 워크아웃이다(요약 표 '앞발 판별이 3D 기준과 다른 사이클').
- 엄격 카운트 min(L, R)(두 쪽 다 끝나야 1)에 MediaPipe 좌우를 쓰면 세트 정확 **0.84**(w00~w15 0.79) — floor(사이클/2) 는 0.98. 10걸음 중 한 번의 이름 바뀜이 쌍 하나를 지운다.
- MM-Fit 교대 규칙성: 기준을 믿을 수 있는 w00~w15 에서 교대율 1.000, 세트 끝·세트 도중 모두 floor(k/2) = min(L_k, R_k)(불일치 0). 교대하는 사람에게는 좌우 판별이
  표시를 바꾸지 않는다. 한쪽을 몰아서 하는 세트(5 왼 → 5 오른)는 도중 10순간 중 7순간이 달라지지만(자체 검사 A6) MM-Fit 에는 그런 세트가 없어 빈도를 모른다.

**Q3 컬 팔별 레인 (연구만)** — 팔마다 새 코어(`elbow_L`/`elbow_R`, 35°, DOWN), 완료 = min(nL, nR). 병합 창이 없어 늦은 팔이 두 번 세지 않는다(자체 검사 B4).

| 세트 정확 (과다 / 과소 세트) | 레인 쌍 | `elbow_minside` 사이클 c | `elbow_minside` floor(c/2) |
|---|---|---|---|
| MediaPipe 교대 컬 (정답 쌍 = floor(정답/2), 59) | 0.90 (0 / 6) | 0.02 (58 / 0) | 0.88 (0 / 7) |
| 3D 교대 컬 (59) | 1.00 (0 / 0) | 0.00 (59 / 0) | 1.00 (0 / 0) |
| 합성 동시 컬 — 3D 한 팔을 반대 팔에 거울로, 팔 사이 0 ms / 300 ms (109) | 1.00 / 1.00 | 1.00 / 1.00 | 0.00 / 0.00 |

단일 신호는 **동작 방식을 알아야** 한다 — 동시 컬은 사이클 c, 교대 컬은 floor(c/2) 가 맞고 반대로 쓰면 0.00~0.02 다. 레인은 두 방식 모두를 같은 규칙으로 센다.
대가는 두 팔이 다 보여야 한다는 것: MediaPipe 레인 과소 6세트 중 5세트는 약한 팔이 세트 창 샘플의 85% 미만에서만 보였다(과다는 0). 실제 동시 컬 영상은 없다.

**되는 것 / 안 되는 것**
- 된다: 런지 쌍 표시 floor(사이클/2) — 세트 끝 정확도는 걸음 단위 이상이고(가린 것 포함), 자동 진행 시점·위험(세트 앞 헛사이클)은 걸음 단위 목표 2P 와 똑같다.
  홀수 사이클 상태를 좌우 판별 없이 '반대쪽 차례' 로 보일 수 있다(어느 쪽인지는 말하지 않는다).
- 된다(기록용): MediaPipe 걸음별 좌우 — 정면에서 걸음당 약 97%. 로그·연구 메타데이터로는 쓸 만하다.
- 안 된다(지금): 좌우 판별로 카운트를 정하기(min(L, R)) · 다음 쪽을 이름으로 말하기('왼쪽 차례') — 틀림이 확신 있는 이름 바뀜이라 화면·음성 모두 사용자를
  잘못 이끈다(원칙 #6). 세트 앞 헛사이클의 위상 밀림을 좌우 판별로 바로잡는 것도 같은 이유로 검증되지 않았다.
- 모른다: 앱 권장 B 대각·휴대폰 조건에서의 좌우 판별, 한쪽을 몰아서 하는 사용자의 빈도, 실제 동시 컬에서 레인의 팔 가림 — Gate A 에서 좌우 순서를 자가 라벨로 모아야 답이 나온다.

## 12. 폰 검증 Gate A 파이프라인 (2026-09-24, spec §61)

앱의 렙 검증 모드(표시 파일 `rep_validation.on` — 숫자 숨김·자동 진행 끔·음성 끔, 세트 로그에 `validation`·`image`·프레임별 `xy`·`w`·`up`)로 찍은
세트를 PC 에서 몇 줄로 보고서까지 만든다. 절차·주의(설치 함정, 집계 규칙)는 `GATE_A_RUNBOOK.md`.

```bash
python pull_phone.py validation on                  # 폰 표시 파일 — 기기에서 지우는 명령은 이것의 off 뿐
python pull_phone.py pull --out ../../data/phone/20261001   # posture_logs 를 같은 구조로 복사 + pull_manifest.json (기기에서 지우지 않음)
python gate_a.py run ../../data/phone/20261001 --plan ../../data/phone_rep/plan_gateA.csv --out ../../data/phone/20261001_gateA
python gate_a.py dry-run --out <임시 폴더>           # 폰·데이터셋 없이 전 과정 검사(합성 픽스처)
```

| 파일 | 역할 | 검증 |
|---|---|---|
| `pull_phone.py` (새) | adb 찾기(`--adb` → `$ADB` → PATH → `$ANDROID_HOME`·`$ANDROID_SDK_ROOT` → `%LOCALAPPDATA%\Android\Sdk` → `~/Library/Android/sdk` → `~/Android/Sdk`), `devices`, `pull`(세트 로그·`rep_truth.csv`·`labels/set_labels.jsonl`·`feedback-*.jsonl` 만, 크기 대조, 매니페스트), `validation on/off/status`. 표준 라이브러리만, 윈도우·맥·리눅스 같은 명령. 기기의 원격 셸 명령 중 삭제·이동은 표시 파일 `rm -f` 하나만 통과하고 나머지는 코드가 막는다 | `--self-test` 20/20 (찾기 순서 8단, 가짜 adb 스크립트로 devices·표시 파일·회수·크기·'기기 파일이 줄지 않음'·rm 은 표시 파일 하나뿐, 로그 폴더 없음 안내) |
| `gate_a.py` (새) | `run`: `setlog_captures.build` → `score_phone_reps.score`(Gate A 점추정, live·hysteresis·신호 후보) → `phone_noise.run` → 프레이밍(종목 × 세로/가로: 필수 관절이 화면 밖이거나 가시성 < 0.5 인 검출 프레임 비율·긴 잘림·관절군·몸 높이 — 스쿼트·런지류 = `rehab_diagnose.LOWER`, 컬 = 어깨·팔꿈치·손목) → 피처 무결성(`.cap` 을 `Replay --dump-features` 로 다시 계산해 로그 신호와 비교, 각도 0.05° · 거리 0.001) → `report.md`(설계 §7 나가는 조건 점검표, 틀린 세트의 자동 원인 후보, 표). `dry-run`: 픽스처 → 같은 파이프라인 → 기대값 검사, 실패하면 종료 코드 1 | `dry-run` 18/18 (아래) |
| `make_phone_fixture.py` (새) | 데이터셋 없이 절차적 33관절 골격(MediaPipe 순서·부호)으로 검증 모드 세션을 만든다 — 스쿼트·런지(왼·오른 번갈아) × 세로 360×640·가로 640×360, 중간 40% 동안 무릎·발목이 화면 밖인 세로 스쿼트, 앞뒤 준비·정리 동작 세트, 7걸음(짝 없는 한쪽) 런지. 피처는 `Replay --dump-features`(앱 소스), reps 블록은 지금 빌드 live 재생(런지는 `side_pair` 필드), 세트 `view` 는 PostureView.kt 상수로 분류, 로그는 `encode_setlog`(코틀린 골든과 바이트 일치 검사된 사본). 계획표(`rep_validation_plan.FIELDS`, 집계 = 정답)·자가 라벨도 쓴다. 결정적(같은 seed → 같은 바이트) | 드라이런이 쓴다 |
| `setlog_captures.py` (개정) | 검증 모드 로그(`xy`·`w` 가 있는 프레임) → 랜드마크 캡처 `<set_id>.cap` 도 쓴다: H `source=phone-setlog`·`setId`·`exercise`·`imageW`·`imageH`·`validation=1`, 검출 프레임마다 `U`(up 그대로) + `F`(vis = 로그 vis, pres = nan, 월드 = `w`), 미검출 = `F t 0`. index 에 `landmarkCapture`·`validation`·`imageW`·`imageH` | `--self-test` 49/49 (+3: 골든 넷째 줄 → `.cap` → `capture_format.read_capture` 왕복의 xy·w·up·미검출, 검증 아닌 줄은 캡처 없음, 재생기가 U 줄 캡처를 읽음) |
| `capture_format.py` (개정) | `U <tMs> <x>,<y>,<z>` 줄(바로 다음 F 줄의 up) 정의, `up_line`, 표준 라이브러리 리더 `read_capture` | setlog_captures 자가 검사 |
| `replay-jvm` `Replay.kt` (개정) | 캡처의 `U` 줄(다음 F 줄 하나에만, 시각이 다르면 멈춘다 — 없으면 전처럼 SCREEN_UP), `nan` 숫자 읽기(capture_format 이 쓰는 표기), 프레임 피처에 `ViewEstimator.frameFeatures` 추가(앱과 같은 사전 — 카운터 신호는 그대로), `--dump-features <cap> <out.jsonl>`, 결과 줄에 카운터 실제 구성 `refractoryMs`·`maxGapMs`·`completeOnReturn`·`romValidated` | `gradle test` 46/46 (앱 테스트 41 + `ReplayCaptureTest` 5: U 줄 없음 = 명시적 화면 세로축, U 줄은 다음 프레임에만·관절각 불변·up 의존 피처는 바뀜, 시각 어긋남 거부, dump = 재생 함수 값·미검출·가시성 < 0.5, 구성 필드) |
| `phone_noise.py` (개정) | 본문을 `run(inputs, out, signal)` 으로 뺐다(gate_a 가 부른다). CLI 출력은 같다 | 드라이런 |

**드라이런 결과** (`gate_a.py dry-run`, 합성 7세트 — 도구 검사이지 카운터 성능이 아니다): 계획 짝짓기 7/7, 정답 전부 집계, app = live 재생 7/7,
깨끗한 6세트는 live = 정답, 잘림 세트(세로 스쿼트 8회)는 live 3 · hysteresis 4. 프레이밍: 그 세트 잘림 0.385(무릎 0.26 · 발목 0.385 · 엉덩이 0.02),
가장 긴 잘림 12.7 s, 나머지 0 → 스쿼트 세로 칸 0.192 · 가로 0. 무결성: 732 프레임 최대 차 5×10⁻⁶(로그 피처 5자리 반올림), 음성 대조(한 프레임 +1°)는 잡힌다.
실기기 로그는 좌표를 반올림하기 **전**에 피처를 계산하므로 최대 차가 이보다 크다(허용 오차 0.05° 는 그 몫).

