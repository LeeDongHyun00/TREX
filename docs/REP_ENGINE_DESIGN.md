# 휴대폰 횟수 엔진 설계 — 현재 카운터를 무엇으로, 어떻게 바꾸는가

- 작성일: 2026-09-24 · 기준 커밋: `07e6364` · 성격: **실행 설계(제안)**. 앱 코드는 아직 바꾸지 않았다.
- 근거: `research/external_rep_replay/README.md` §4·§7(현재 엔진의 MM-Fit 재생과 원인 귀속), 같은 폴더 `prototype_counter.py`(이 설계의 카운터 코어 프로토타입 실측).
- 전제: 이 문서의 모든 실측은 MM-Fit 자체 3D 포즈(휴대폰·MediaPipe 아님) 위에서 앱 간격 300ms 로 잰 것이다. 휴대폰 조건이 더하는 몫은 §7 검증 사다리로 따로 잰다.

---

## 0. 요약

현재 카운터(`RepCounter(completeOnReturn = true)` = `ReturnRepTracker`)가 실제 연속 동작에서 반복을 놓치는 원인은 촬영이 아니라 **네 가지 설계 선택**이다 — ① 런지 신호(`knee_out_mean`)가 앞으로 딛는 런지에서 거의 움직이지 않고 좌우가 상쇄된다, ② 세트 시작 전에 600ms 이상 멈춘 준비 자세가 있어야 기준을 잡는다(준비 자세가 잘못 잡히면 세트 전체가 0회), ③ 상단에 300ms 이상 머물러야 복귀로 인정한다(빠른 템포에서 탈락), ④ 3점 중앙값 평활이 한 샘플짜리 바닥을 지운다. 이 넷은 모두 카메라와 무관하게 재현됐고, 같은 300ms 신호에 평활·준비 자세 없는 단순 카운터를 돌리면 반복의 99% 가 들어 있다.

설계는 **엔진을 새로 만들지 않고 네 곳을 바꾼다**: 신호 선택 규칙(회전 불변 관절각 우선, 교대 종목은 좌우 개별), 카운터 코어(복귀 히스테리시스 — 안정 준비 자세·상단 체류·평활 요구 제거), 세트 시작 확정(첫 두 사이클 동시 확정), 그리고 카운트의 신뢰 등급을 화면·진행 조건에 연결한다. 나머지(추론 정책, ROM, 로그·라벨 루프)는 보강이다.

| | 현재 엔진 | 제안 코어(프로토타입) |
|---|---|---|
| 스쿼트 세트 정확 일치 / 재현율 | 0.53 / 0.90 | **0.97 / 0.997** |
| 런지 세트 정확 일치 / 재현율 | 0.00 / 0.10 | **0.95 / 0.98** (신호 `knee_mean`) |
| 덤벨 컬 세트 정확 일치 / 재현율 | 0.42 / 0.86 | **0.98 / 0.998** (팔별 카운터) |
| 세트 안 과다 카운트 세트 | 0 | 0 |
| 세트 직전 10초 헛카운트 / 세트 | 0.00 / 0.00 / 0.07 | 0.00 / 0.00 / 0.22 (컬 게이트 50° 면 0.12) |
| 휴식 중 헛카운트 / 분 | 0.20 / 0.19 / 1.53 | 0.03 / 0.03 / 3.0 (컬 50° 면 1.8) |

컬의 음성 구간이 제안 코어의 약점이다. 앱은 사용자가 시작한 세트 안에서만 세므로 "휴식 중" 은 직접 해당하지 않지만, "세트 직전 10초" 는 시작 버튼 뒤의 준비 움직임에 해당하므로 §4.3·§4.4 에서 다룬다.

## 1. "휴대폰에서 쓸 만하다" 의 정의 (수용 기준, 사전 등록)

카운트는 §42 이후 **세트 진행 조건**이다(목표 횟수 도달 → 다음 세트). 그래서 과다 카운트는 세트를 조기에 끝내고, 미달은 사용자가 손으로 고쳐야 한다. 두 오류의 비용이 다르므로 기준도 비대칭이다.

| 지표 | 기준 | 이유 |
|---|---|---|
| 세트 정확 일치 | ≥ 0.90 | 열 세트 중 아홉은 손을 대지 않아야 "자동" 이다 |
| 세트 ±1 | ≥ 0.98 | 나머지는 한 번의 수정으로 끝나야 한다 |
| 세트 안 과다 카운트 | ≤ 2% 세트 | 조기 종료는 미달보다 나쁘다 |
| 세트 시작 뒤 첫 반복 전 헛카운트 | ≤ 0.05 / 세트 | "시작하자마자 1회" 는 신뢰를 바로 깎는다 |
| 마지막 반복 | 다음 하강 없이 확정 | §42 의 복귀형 도입 이유 — 유지 |
| 항상-목표치 기준선 | 같은 표에 병기 | 세트의 94% 가 정확히 10회인 데이터에서는 "항상 10" 이 강한 기준선이다 |
| 통계 단위 | 사람(피험자) | P0·P1 두 명이 MM-Fit 반복의 56% — 세트 단위 신뢰구간은 절반으로 좁게 나온다 |

이 기준을 **MM-Fit 봉인 피험자 → MM-Fit 영상(MediaPipe) → REHAB24-6 경계 → 휴대폰 자가 라벨** 순으로 통과해야 하고(§7), 마지막 단계를 지나기 전에는 카운트가 beta 로 남는다(원칙 #2). 기준값 자체는 제안이며 첫 휴대폰 라벨 세트로 현실성을 재본다.

## 2. 현재 엔진의 오차 모델 (실측)

`ReturnRepTracker` 의 내부 상태를 매 프레임 읽어 놓친 반복 712회에 원인을 하나씩 붙인 결과(`README.md` §7):

| 원인 | 스쿼트 | 런지 | 컬 | 어디서 온 선택인가 | 촬영과의 관계 |
|---|---:|---:|---:|---|---|
| 신호 폭 부족 (`knee_out_mean` 반복당 0.052 < 게이트 0.10, 좌우 상쇄, 두 반복 주기) | — | 513 | — | AIHub 0.6초 표본 설문 1위 | 무관 (회전·기기 없이 재현) |
| 상단 체류 < 600ms → 복귀 미인정 | 26 | — | 29 | `ReturnRepTracker` 설계(2026-09-11, 합성 테스트만) | 무관 |
| 3점 중앙값이 한 샘플 바닥을 지움 | 7 | — | 40 | v4 폰 재생 규약 | 무관 |
| 준비 자세에 잘못 잡힌 기준 (예: 덤벨 든 채 팔꿈치 125°) | 8+ | — | 2세트 전체 | `ReturnRepTracker` 앵커 규약 | 무관 |
| 0.5초 여유가 마지막 복귀를 자름 | 20 | — | 14 | 평가 인공물 | — |
| 앵커 없음·늦음·드리프트·끊김 (기기에 민감할 수 있는 것) | ≤ 4 | | | | 유일하게 촬영이 관여할 수 있는 몫 |

불응기 1.2s 와 1.5s 끊김 초기화는 비용 0 이었다. 시점 변환 26개에서 스쿼트·컬은 한 세트도 바뀌지 않았다(세 관절 각은 회전 불변). 런지만 IMU 없는 기울기에서 흔들린다(재현율 0.06~0.35, 최선도 `knee_mean` 0.84 에 못 미침).

**AIHub 에서 온 것과 아닌 것.** 횟수를 세는 논리는 AIHub 에서 오지 않았다(반복 라벨이 없다). AIHub 에서 온 것은 신호 선택(설문)과 ROM 임계값뿐이고, 그중 런지 신호가 무너졌다. 스쿼트·컬의 손실은 AIHub 와 무관한 추적기 설계다.

## 3. 설계 원칙

1. **모집단 파라미터.** 게이트·비율·창은 물리 단위의 모집단 값이고, 특정 사용자나 특정 데이터셋(MM-Fit 포함)에 맞추지 않는다. 데이터셋은 선택지 중에서 고르고 효과를 재는 데만 쓴다(README §5, `CLAUDE.md` #4).
2. **판정하지 않은 것을 판정한 것처럼 말하지 않는다.** 카운트에는 신뢰 등급이 붙고, 낮은 등급은 진행 조건에서 빠진다(#1·#2).
3. **카운트와 ROM(가동범위) 유효성은 분리.** ROM 은 카운트에서 빼지 않는다(§42) — 유지.
4. **엔진 파일은 앱 최상위에 의존하지 않는다.** 새 코어는 `posture/` 안, `RepCounter` 의 공개 API(`onFrame → Boolean`, `lastCycleMin/Max`, `periodMs`, `repTimesMs`)를 유지해 `PostureLive` 변경을 최소화한다.
5. **검증 사다리 없이 ship 없음.** 휴대폰 자가 라벨(`rep_truth.csv`)이 최종 게이트다.

## 4. 아키텍처

```
카메라 → PostureAnalyzer(300ms, IMU up) → 프레임 피처
      → [4.1 신호 계층] 종목별 신호(좌/우/평균) + 물리 범위·가시성 게이트
      → [4.2 카운터 코어] 복귀 히스테리시스 상태기계 (좌우 개별 인스턴스)
      → [4.4 교대 병합] 반대쪽 발화 W ms 안이면 같은 반복
      → [4.3 시작 확정·일관성] 첫 두 사이클 동시 확정, 이후 진폭 일관
      → [4.8 신뢰 등급] 확정 / 감지 중 / 저신뢰(샘플 부족·발열)
      → 세션 진행(확정만) · HUD · 음성 · 세트 로그 · ROM 참고(4.5)
```

### 4.1 신호 계층 — 종목별 신호 선택 규칙

규칙(우선순위 순):
- **R1 회전 불변 관절각 우선.** 세 관절 각(`angle3`)은 카메라 방향·IMU 오차·좌우 반전에 영향받지 않는다. 뷰·중력 의존 신호(`knee_out_*`, `*_h_*`, `palm_*`)는 IMU 가 있고 실기기 검증을 통과한 종목에서만 쓴다.
- **R2 교대 종목은 좌우 개별.** 좌우 평균은 교대 동작에서 진폭을 반으로 줄이고(컬 평균 ~50° vs 팔별 ~95°) ROM 기준에 구조적으로 닿지 못하게 한다. `ExerciseProfiles` 가 교대로 표시한 종목(런지 4종·덤벨 컬·스탠딩 니업)은 좌·우 신호에 카운터를 각각 두고 §4.4 로 병합한다. 양쪽이 함께 굽는 종목(스쿼트, 전진 런지의 무릎)은 평균을 쓴다.
- **R3 진폭 게이트는 물리 단위, 하한은 기기 잡음 바닥.** 각도형 35° 는 플랭크 유지 중 폰 잡음(5초 창 10~30°) 실측에서 온 하한이라 유지한다. 종목별로 더 높은 게이트를 두려면 AIHub 정상 반복의 ROM 분포에서 근거를 대고(§4.4 컬), MediaPipe 가 깊은 굴곡을 얕게 읽는 편향(+10~30°, `docs/bridge-frames.full-sweep.v1.json`)만큼 여유를 남긴다.
- **R4 신호 교체는 연속 데이터에서 검증.** AIHub 0.6초 표본의 "클립 간 일관성" 은 연속 동작에서의 성립을 보장하지 않았다(런지). 후보는 MM-Fit 봉인 피험자에서 재고, 순위가 휴대폰 라벨 세트에서도 같을 때 채택한다.

세 종목의 적용:

| 종목 (앱 → AIHub) | 현재 | 제안 | 근거 |
|---|---|---|---|
| 바벨 스쿼트 | `knee_mean` 35° | 유지 | 0.997 |
| 런지 → 스텝 포워드 다이나믹 런지 | `knee_out_mean` 0.10 | **`knee_mean` 35°** (양 무릎이 함께 굽는다) | 0.10 → 0.98. `knee_minside` 는 0.77 로 열세 |
| 덤벨 컬 | `elbow_mean` 35° | **`elbow_L`·`elbow_R` 각각 + 병합** | 0.86 → 0.998, ROM 팔별 판정 가능 |

나머지 서서 종목 15개는 같은 규칙으로 재검토 대상이다. 특히 R1 에 걸리는 신호(`knee_out`·`hip_below_knee`·`palm_h_sh`·`forearm_vert`·`elbow_wrist_h` 계열)와 R2 에 걸리는 교대 종목(바벨 런지·사이드 런지·크로스 런지·스탠딩 니업)을 먼저 본다. 검증 데이터가 없는 종목은 바꾸지 않고 beta 로 둔다.

### 4.2 카운터 코어 — 복귀 히스테리시스

한 반복 = 휴식 → 하강 → 반전 → 복귀. 준비 자세를 미리 요구하지 않고, 상단에 머물 것을 요구하지 않고, 평활하지 않는다.

```
REST : r = 마지막 발화 이후 최댓값 (휴식 기준). v ≤ r − h → DESC, r0 = r
DESC : m = 최솟값 추적. v ≥ m + h → ASC (반전 확정 = 후보), a = v
ASC  : v ≥ m + (1 − f)·(r0 − m) → 발화 (복귀 f 만큼 남긴 지점, f = 0.25)
       v ≤ a − h (복귀 전 재하강) → 발화 후 곧바로 DESC (불완전 복귀 폴백: 다음 사이클이 시작됐다는 증거)
       그 외 a = max(a, v)
발화 후: 직전 발화와 refractory 미만이면 잡음으로 버림. 사이클 극값 (m, r0) 을 ROM 용으로 노출.
```

| 파라미터 | 값 | 출처 |
|---|---|---|
| h (최소 스윙) | 각도 35° / 거리 0.25~0.30 | 현재와 같음 — 폰 잡음 바닥 실측 |
| f (복귀 잔여 비율) | 0.25 | 설계값. "준비 위치로 돌아왔을 때 완료" 의 완화판 — 상단 체류를 요구하지 않는다 |
| refractory | 0.8 s | 설계값. 현재 1.2 s 는 비용 0 이었으나 1.2 s/반복 템포에 닿는다 |
| maxGap | 1.5 s | 현재와 같음 — 가림·일시정지 뒤 사이클을 잇지 않는다 |
| 평활 | 없음 (각도형) | 3점 중앙값이 재현율 0.997 → 0.975, 컬 정확 일치 0.98 → 0.95 로 깎는다 |

마지막 반복은 복귀 지점에서 확정되므로 다음 하강이 필요 없다(§42 요구 유지). 세트 종료(사용자 종료·시간 만료) 시 ASC 상태의 후보는 **세지 않고** 로그에 "미완 후보" 로 남긴다 — 절반만 올라온 동작을 한 회로 만들지 않는다.

평활을 없애면 한 샘플짜리 튐(예: 관절 추정 실패)이 사이클로 보일 수 있다. 폰에서 이 튐의 빈도는 아직 재지 않았다. 대비책은 평활이 아니라 **문맥 조건부 튐 제거**다: 양 이웃 샘플이 모두 휴식 기준 근처(r − 0.3h 이상)일 때만 가운데 샘플을 이웃 중앙값으로 대체한다. 진짜 바닥의 이웃은 하강·상승 중이라 휴식 근처가 아니므로 살아남는다. 이 필터는 §7 의 영상·휴대폰 단계에서 튐 빈도를 잰 뒤 켠다.

### 4.3 세트 시작 확정과 진폭 일관성

`ReturnRepTracker` 의 "안정 준비 자세" 는 시작 전 오염(걸어오기·기구 잡기)을 막으려는 장치였고, 그 값이 잘못 잡히면 세트 전체를 잃었다. 대신 **사이클 두 개의 일관성**으로 시작을 확정한다:

- 첫 발화는 보류한다. 4초 안에 진폭이 0.5~2배인 두 번째 사이클이 오면 둘을 함께 발표한다(HUD 는 0 → 2). 오지 않으면 버린다.
- 이후 발화는 직전 발표 사이클과 진폭 비 0.5~2배 안이어야 한다. 고립된 한 번의 큰 움직임은 반복이 아니다.

효과(프로토타입): 휴식 헛카운트 스쿼트·런지 0.35 → 0.03/분, 컬 5.6 → 3.0/분. 세트 직전 10초 헛카운트 런지 0.23 → 0.00/세트, 컬 0.56 → 0.22/세트. 세트 재현율은 스쿼트 0.997 유지, 런지 0.990 → 0.981, 컬 1.000 → 0.998. 비용은 첫 반복이 두 번째와 함께 표시되는 지연 하나다 — 음성은 "둘" 부터 세되 화면에는 "첫 두 회는 함께 확정돼요" 를 준비 안내에 넣는다.

### 4.4 교대 종목의 정의와 병합

- 정의: **한쪽 1회 = 1회**(MM-Fit 라벨과 같고, 왼·오른 5+5 = 10). 양쪽을 동시에 하면 1회. 편집기(`WorkoutEditor`)에 "양쪽 1회 = 1회" 옵션을 두되 기본은 한쪽 1회다 — 기본값을 바꾸면 기존 목표 횟수의 의미가 두 배로 달라진다(§42 의 "기존 목표를 임의로 두 배로 만들지 않는다").
- 병합: 좌·우 카운터의 발화가 W = 500ms 안에 겹치면 하나로 센다. 교대 컬의 좌우 간격(약 1.9초)과 동시 컬(≤ 300ms)을 나눈다.
- 컬의 음성 구간: 팔별 카운터는 휴식 중 팔 움직임(물 마시기·기구 조정)에 두 배로 노출된다. 게이트를 팔별 50° 로 올리면 세트 직전 헛카운트 0.22 → 0.12/세트, 휴식 3.0 → 1.8/분이고 세트 재현율은 1.000 이다. **근거는 AIHub 다**: 정상 컬 반복의 팔꿈치 바닥 p90 이 81.3°, 휴식 팔꿈치가 150~160° 이므로 정상 반복의 스윙은 약 70° 이상이고, 50° 는 MediaPipe 가 깊은 굴곡을 얕게 읽는 편향(+10~30°)을 뺀 값이다. 다만 MediaPipe 정면 컬의 실제 스윙 분포는 미측정이라 **35°(잡음 바닥)~50° 사이에서 영상 단계(§7-2) 결과로 확정**한다. 어깨각 < 90° 문맥 게이트는 머리 위 신전 오카운트만 줄이고(26.9 → 2.1/분) 휴식에는 효과가 없어 채택하지 않는다.

### 4.5 ROM 유효성 — 정의를 고치고 기준값은 두지 않는다

- 카운트에서 빼지 않는다(현재와 같음). 리포트의 유효/파셜 표시와 음성은 지금처럼 검증된 5개 바닥 종목만.
- **원값으로 판정.** ROM 임계값은 평활하지 않은 AIHub 값에서 뽑았는데 지금은 평활된 사이클 최저값과 비교한다. 코어가 평활을 없애면 저절로 맞는다(스쿼트 유효 161/576 → 약 346/576, 근사).
- **교대 종목은 쪽별.** 팔별 카운터의 사이클 극값을 그 팔의 ROM 기준과 비교한다(AIHub 81.3° 기준으로 MM-Fit 한 팔 컬 601/604 통과 — 기준값을 바꾸지 않아도 정의만 고치면 맞는다).
- 런지의 ROM 은 신호가 바뀌므로 `knee_out_mean −0.0076` 을 버리고 **없음(항상 유효 취급)** 으로 둔다. AIHub GT 3D 로 다시 뽑은 `knee_mean` 후보(146.6°)는 기준 구실을 못 해 쓰지 않는다(§10). MM-Fit 에서 뽑지 않는다.
- MediaPipe 편향(+10~30°) 때문에 AIHub 3D 기준값은 휴대폰에서 정상 반복을 "얕음" 으로 볼 수 있다. 재보정(§9) 전에는 ROM 이 헤드라인이 되지 않는다.

### 4.6 추론 정책 — 300ms 유지, 필요할 때만 부스트

- MM-Fit 템포(반복당 1.2~4초)에서는 300ms 로 충분했다(200ms 부스트가 결과를 바꾸지 않았다). 반복당 4샘플 미만(≈ 1.2초/반복 미만)에서 카운트가 무너진다는 AIHub 서브샘플링 실측(`REP_SIGNALS.md`)은 유효하다.
- 부스트: 카운터의 주기 추정이 1.5초 아래로 내려오면 RECORDING 간격을 200ms 로 내린다(발열 NONE·LIGHT 에서만). MediaPipe full 이 GPU 에서 약 100ms 이므로 5fps 는 예산 안이다.
- 발열 감속(MODERATE 이상, 간격 ×1.5~3)이나 부스트 불가 상태에서 반복당 샘플이 5 미만이면 카운트를 **저신뢰**로 강등하고 "정확한 카운트를 위해 조금 천천히" 를 한 번 말한다. 카운트를 조용히 놓치는 것보다 낫다(#1).
- 실제 추론 간격·발열 상태를 세트 로그에 남긴다(§4.9).

### 4.7 휴대폰 특유 조건의 처리

| 조건 | 처리 |
|---|---|
| IMU up 벡터 | 각도 신호는 무관. 뷰 의존 신호는 IMU 없으면(`upFromGravity = false`) 카운터를 만들지 않고 "이 종목은 센서가 필요해요" 로 밝힌다 |
| 검출 끊김·가림 | 값이 없으면 카운터 일시정지(현재), 1.5초 초과면 사이클 리셋(현재). 필요한 관절이 화면 밖이면 커버리지 안내가 카운트보다 우선(현재 규칙 유지) |
| 물리 범위 밖 값 | `plausibleMin/Max` 게이트 유지(손목 붕괴 등) |
| 한 샘플 튐 | §4.2 문맥 조건부 튐 제거 — 빈도 측정 후 |
| 회전·일시정지·재배치 | `resetCycle()` 유지. 확정된 수는 지키고 진행 중 사이클만 버린다 |
| 앞·뒤 카메라 | 각도 신호는 좌우 반전에 무관. 교대 병합도 대칭이라 무관 |
| GPU/CPU 델리게이트 차이 | 영상 단계에서 두 델리게이트 결과를 비교해 크기를 기록한다(설계상 처리 없음) |
| 세트 시작 오염 | §4.3 일관성 확정으로 대체. 준비 카운트다운 중에는 지금처럼 세지 않는다 |
| 세트 종료 시 미완 후보 | 세지 않고 로그에 남긴다(§4.2) |

### 4.8 신뢰 등급과 UX

카운트는 세 등급으로 나뉜다.

| 등급 | 조건 | 표시 | 진행 조건 | 음성 |
|---|---|---|---|---|
| 확정 | 일관성 확정 + 반복당 샘플 ≥ 5 + 발열 NONE/LIGHT | 숫자 | 포함 | 숫자 |
| 감지 중 | 첫 사이클 보류 중, 또는 ASC 후보 | 숫자 옆 흐린 "+1" | 제외 | 없음 |
| 저신뢰 | 반복당 샘플 < 5, 발열 감속, 부스트 불가 | 숫자 + "참고" 배지 | 제외 → 목표 도달 시 확인 버튼 | 톤만 |

- 목표 도달 자동 진행은 확정 등급만 센다. 저신뢰 상태에서 목표에 닿으면 "N회 감지 — 맞으면 다음 세트" 확인을 띄운다.
- 하단 횟수 수정 UI 는 유지한다. 사용자가 고친 값은 자가 라벨(`source = edited`)로 저장된다(현재 §30 경로).
- 교대 종목은 준비 안내에 "한쪽 1회를 1회로 세요" 를 넣는다(정의를 미리 밝힌다).

### 4.9 로그·라벨 루프 (측정 없이는 개선이 없다)

세트 로그(`SetLog`)에 추가: 카운터 상태 전이(REST/DESC/ASC 시각과 값), 후보·확정·병합·기각 이벤트와 사유, 사이클 극값(원값), 실제 추론 간격 분포, 발열 상태, 부스트 여부, 세트 종료 시 미완 후보. 완료 화면의 실제 횟수 입력(§30, `rep_truth.csv`)이 정답이다. `research/external_rep_replay` 하네스가 이 로그를 그대로 재생해 회귀를 잡는다.

## 5. 프로토타입 실측 (MM-Fit 자체 3D 포즈, 300ms, 21 워크아웃)

`research/external_rep_replay/prototype_counter.py`. 상수는 전부 §4 의 모집단 값이고 MM-Fit 에 맞추지 않았다. 컬 게이트 열은 민감도 확인이지 적합이 아니다(근거는 §4.4).

| 변형 | 종목 | 세트 | 재현율 | 정확 | ±1 | 과다 세트 | 휴식 헛카운트/분 | 세트 직전 10s /세트 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 현재 엔진(live) | 스쿼트 | 64 | 0.901 | 0.53 | 0.81 | 0 | 0.20 | 0.00 |
| 현재 엔진(live) | 런지 | 62 | 0.099 | 0.00 | 0.00 | 0 | 0.19 | 0.00 |
| 현재 엔진(live) | 컬 | 59 | 0.855 | 0.42 | 0.64 | 0 | 1.53 | 0.07 |
| 코어만 (hys300) | 스쿼트 | 64 | 0.997 | 0.97 | 1.00 | 0 | 0.35 | 0.00 |
| 코어만 | 런지 `knee_mean` | 62 | 0.990 | 0.97 | 0.97 | 0 | 0.35 | 0.23 |
| 코어만 | 컬 팔별 | 59 | 1.000 | 0.98 | 1.00 | 1 | 5.62 | 0.56 |
| 코어 + 3점 중앙값 | 스쿼트 / 런지 / 컬 | | 0.975 / 0.938 / 0.992 | 0.83 / 0.79 / 0.95 | | 0 | | |
| **코어 + 시작 확정** | 스쿼트 | 64 | 0.997 | 0.97 | 1.00 | 0 | 0.03 | 0.00 |
| **코어 + 시작 확정** | 런지 `knee_mean` | 62 | 0.981 | 0.95 | 0.97 | 0 | 0.03 | 0.00 |
| **코어 + 시작 확정** | 컬 팔별 35° | 59 | 0.998 | 0.98 | 1.00 | 0 | 3.01 | 0.22 |
| 코어 + 시작 확정 | 컬 팔별 50° | 59 | 1.000 | 1.00 | 1.00 | 0 | 1.84 | 0.12 |
| 코어 + 시작 확정 | 컬 팔별 60° | 59 | 1.000 | 1.00 | 1.00 | 0 | 1.28 | 0.03 |

- 200ms 간격은 이 데이터에서 결과를 바꾸지 않았다(가장 빠른 세트도 반복당 4샘플 이상).
- 타 종목 세트에서의 오카운트(예: 스쿼트 카운터가 런지에서 20/분, 컬 카운터가 로우에서 34/분)는 앱에서 사용자가 종목을 고르므로 직접 해당하지 않지만, 세트 중 다른 동작을 끼워 넣으면 그대로 센다는 뜻이다. §4.3 의 진폭 일관성이 일부를 거른다.
- 항상-10 기준선: MAE 0.08 / 0.10 / 0.15 (정확 일치 0.95 / 0.95 / 0.92). 제안 코어의 정확 일치 0.97 / 0.95 / 0.98 은 이 기준선을 처음으로 넘는다.

## 6. 구현 계획

| 단계 | 내용 | 파일 | 완료 기준 |
|---|---|---|---|
| 0. 측정 배관 | 세트 로그에 §4.9 필드, 하네스가 세트 로그를 재생 | `PostureSetLog.kt`, `research/external_rep_replay/` | 현재 엔진의 로그 재생이 앱 카운트와 일치 |
| 1. 신호 교체 | 런지 `knee_mean`, 컬 팔별(+병합), 런지 ROM 없음 | `RepCounter.kt`(RepSignals), `PostureLive.kt`(좌우 인스턴스) | 코어 그대로도 런지 0.10 → 0.84, 컬 0.86 → 0.87(진단 실측) |
| 2. 코어 교체 | `ReturnHysteresisCounter`(§4.2) + 시작 확정(§4.3), 평활 제거, `RepCounter` API 유지 | `posture/` 신규 파일, `RepCounter.kt` | 기존 픽스처 유지: baseline1 4렙, 폰 라벨 세트 9/12 → **≥ 11/12** 목표, 플랭크 잡음 0; MM-Fit 봉인 피험자 정확 일치 ≥ 0.9 |
| 3. 신뢰 등급·UX | §4.8 등급, 진행 조건 = 확정만, 준비 안내 문구, 부스트(§4.6) | `PostureLive.kt`, `LiveWorkoutHud.kt`, `InferencePolicy.kt`, `TrexApp.kt` | JVM 테스트 + 실기기 세트 흐름 1건 |
| 4. 휴대폰 검증 | 권장 방향에서 세 종목 라벨 세트 각 ≥ 10세트, 두 사람 이상; 튐 빈도 측정 → §4.2 필터 결정; 컬 게이트 확정 | 로그·`rep_truth.csv` | §1 기준 통과 시 beta → ship 검토. 미통과면 원인 귀속(§2 표) 후 다음 단계 |

반전형(`RepCounter` 기본 구성)은 연구 재생 파리티용으로 남긴다. 단계 1 은 코어와 독립이라 먼저 내보낼 수 있다.

## 7. 검증 사다리

| 단계 | 데이터 | 답하는 질문 | 상태 |
|---|---|---|---|
| 1 | MM-Fit 자체 3D 포즈 (CC BY 4.0) | 카운팅 논리가 연속 동작에서 성립하는가 | §5 완료 — 봉인 피험자(w00·w05·w12·w13·w20) 분리 보고는 구현 뒤 |
| 2 | MM-Fit 영상 → MediaPipe(앱 모델·300ms) | MediaPipe 가 더하는 오차(정면 컬 팔꿈치 스윙 압축, 튐 빈도, 델리게이트 차이) | 영상 다운로드 중 — `extract_mediapipe.py` 준비됨 |
| 3 | REHAB24-6 (CC BY-NC, 측정 전용) | 반복 경계가 맞는 순간에 찍히는가(경계 F1), 세로 프레이밍 손해 | 준비됨 |
| 4 | 휴대폰 자가 라벨 세트, 앱 권장 방향 | IMU·GPU·지터·발열·거치까지 포함한 최종 정확도 | 수집 필요 — **ship 게이트** |

각 단계에서 §1 지표를 항상-목표치 기준선과 나란히, 사람 단위로 보고한다. 단계 2 에서 새로 생긴 놓침이 기기·추정기에 민감한 원인(앵커 없음·드리프트·끊김·진폭 부족)에 몰리면 그때 "촬영 조건" 을 원인으로 인정한다.

## 8. 미해결과 위험

- **컬의 음성 구간.** 팔별 카운터 + 시작 확정으로도 세트 직전 헛카운트 0.22/세트(35°)·0.12(50°)다. 기준 0.05 에 못 미친다. 남은 후보: 준비 카운트다운 종료 직후 1초 무시, 두 사이클 확정 창 4초 → 3초, 편집기의 교대/동시 선택에 따른 병합창 조정. 휴대폰 라벨 세트에서 실제 발생률을 본 뒤 정한다.
- **평활 제거의 폰 위험.** 폰 튐 빈도가 미측정이다. 튐이 잦으면 §4.2 조건부 필터를 켠다. 그래도 안 되면 "3점 중앙값은 반복당 샘플 ≥ 8 일 때만" (v4 이전 규약)으로 돌아가는 것이 차선이다.
- **MediaPipe 편향.** 깊은 굴곡을 얕게 읽는다. 35° 게이트에는 여유가 있지만 컬 50° 게이트와 모든 ROM 기준에는 직접 영향이다. 단계 2 가 답한다.
- **교대 정의.** 한쪽 1회 = 1회는 MM-Fit 과 일치하지만 사용자 기대와 다를 수 있다. 준비 안내로 밝히고 편집기 옵션으로 연다.
- **런지 ROM 부재.** AIHub GT 로 `knee_mean` 기준을 다시 뽑기 전까지 런지는 ROM 참고 없이 센다.
- **다른 15개 서서 종목.** 이 설계는 세 종목에서만 실측됐다. 같은 규칙(R1~R4)으로 신호를 재검토하되, 검증 데이터가 없는 종목은 바꾸지 않는다.
- **원칙 #4 와의 긴장.** 컬 게이트 민감도 표는 MM-Fit 에서 잰 것이다. 채택 근거는 AIHub ROM 분포이고 MM-Fit 은 확인용이라는 구분을 문서에 남겼다. 이 구분이 흐려지면 그것이 곧 데이터셋 적합이다.

## 9. 잡음 주입 스트레스 테스트와 "잡음으로 학습" 검토 (2026-09-24 추가)

`research/external_rep_replay/noise_stress.py`. 휴대폰·MediaPipe 조건을 흉내 낸 섭동을 **관절 좌표(mm)** 에 넣고 각도를 다시 계산해 §4 코어(시작 확정 포함)와
현재 라이브 엔진(JVM 재생기)을 같은 데이터로 돌렸다. 폰 잡음의 실제 분포는 아직 없으므로 크기를 단계별로 키워 **무엇이 먼저 무너지는지**를 본 것이다.
학습 데이터가 아니라 안전 여유의 측정이다.

섭동의 크기(각도 환산, 프레임별 σ): 관절 지터 10/20/40mm → `knee_mean` 2.0/3.9/7.7°, `elbow_L` 5.4/10.7/19.5°. 유일한 실측 참조인 MediaPipe−AIHub 3D 잔차는
무릎 σ 14.7°(편향·피험자 차이 포함, 시간 구조 없음)라 실제 프레임 간 지터는 그보다 작을 가능성이 크고, 앱은 `numPoses = 1` 이라 MediaPipe 내부 평활도 받는다.

| 섭동 | 제안 코어 재현율 (스쿼트 / 런지 / 컬 35°) | 제안 코어 정확 일치 | 컬 과다 세트 (35° / 50°) | 컬 세트 직전 헛카운트 (35° / 50°) | 현재 엔진 재현율 (스쿼트 / 런지 / 컬) |
|---|---|---|---|---|---|
| 없음 | 0.997 / 0.981 / 0.998 | 0.97 / 0.95 / 0.98 | 0 / 0 | 0.22 / 0.12 | 0.901 / 0.099 / 0.855 |
| 지터 20mm | 0.997 / 0.986 / 0.975 | 0.97 / 0.97 / 0.76 | 3 / 1 | 1.00 / 0.07 | 0.761 / 0.103 / 0.459 |
| 지터 40mm | 0.997 / 0.978 / 0.968 | 0.97 / 0.89 / 0.19 | 33 / 26 | 4.54 / 2.25 | 0.582 / 0.136 / 0.284 |
| 한 프레임 튐 10% | 0.997 / 0.981 / 0.992 | 0.97 / 0.95 / 0.86 | 4 / 2 | 0.31 / 0.12 | 0.844 / 0.099 / 0.786 |
| 검출 끊김 ~10% | 0.958 / 0.934 / 0.968 | 0.73 / 0.71 / 0.71 | 0 / 0 | 0.14 / 0.07 | 0.732 / 0.098 / 0.710 |
| 추론 간격 300~333ms | 0.997 / 0.981 / 1.000 | 0.97 / 0.97 / 1.00 | 0 / 1 | 0.10 / 0.08 | 0.856 / 0.093 / 0.806 |
| 발열 감속 433~466ms | 0.997 / 0.971 / 0.997 | 0.97 / 0.92 / 0.95 | 1 / 0 | 0.14 / 0.03 | **0.316** / 0.135 / 0.259 |
| 깊은 굴곡 편향 +20° | 0.997 / 0.981 / 1.000 | 0.97 / 0.95 / 1.00 | 0 / 0 | 0.17 / 0.03 | (JVM 경로엔 미적용) |
| 폰 (20mm+튐3%+끊김+간격+편향15°) | 0.955 / 0.942 / 0.937 | 0.70 / 0.71 / 0.51 | 3 / 1 | 0.88 / 0.05 | 0.603 / 0.104 / 0.396 |
| 폰-가혹 (40mm+튐10%+끊김+발열+편향25°) | 0.856 / 0.838 / 0.801 | 0.31 / 0.34 / 0.10 | 4 / 0 | 2.36 / 0.29 | 0.141 / 0.120 / 0.048 |

읽는 법:
1. **편향에는 둘 다 무관하다** — 상대 스윙만 보기 때문이다(ROM 기준만 편향에 노출된다, §4.5).
2. **추론 간격 지터·발열 감속에서 갈린다.** 제안 코어는 거의 안 움직이고(스쿼트 0.997 유지), 현재 엔진은 발열 감속만으로 0.90 → 0.32 로 무너진다 — 상단 체류를 샘플 수로 요구하기 때문이다. 발열 감속은 휴대폰에서 흔한 조건이라 이것만으로도 코어 교체의 근거가 된다.
3. **검출 끊김은 둘 다 잃는다.** 제안 코어도 정확 일치가 0.7 대로 떨어진다. 카운터가 아니라 검출 연속성(프레이밍 안내)과 신뢰 등급(§4.8)이 지렛대다.
4. **팔별 팔꿈치 신호가 잡음에 가장 약하다.** 팔은 짧은 분절이라 같은 관절 지터에 각도 잡음이 두 배(σ 10.7° at 20mm)다. 프레임별 σ ≥ 10° 부터 과다 카운트가 생긴다. 50° 게이트(§4.4)가 세트 직전 헛카운트를 크게 줄이고 과다 카운트를 절반으로 줄이지만 40mm 에서는 못 막는다.
   - 시도하고 **기각한 것**: 최소 사이클 길이 900ms(잡음 사이클이 실제보다 짧다는 가정) — 깨끗한 데이터에서 재현율 1.0 → 0.983 을 잃고 40mm 과다 카운트는 그대로(26). 잡음 사이클은 300ms 표본에서 실제 반복보다 짧지 않다.
   - 시도하고 **기각한 것**: 3점 중앙값 일괄 적용 — 컬 40mm 과다 카운트를 33 → 4 로 줄이지만 재현율 0.968 → 0.900, 스쿼트·런지는 모든 조건에서 손해(깨끗한 런지 0.981 → 0.902).
   - 남는 후보: **잡음 적응 평활** — REST 구간 잔차로 프레임별 σ 를 추정해 팔 신호에서 σ 가 높을 때만 켠다. 켜는 문턱은 영상·폰 로그의 실측 σ 로 정한다.
5. **가혹 조건에서 제안 코어는 천천히 나빠지고(0.86/0.84/0.80) 현재 엔진은 붕괴한다(0.14/0.12/0.05).** 두 경우 모두 §1 기준에는 못 미치므로, 이 크기의 잡음이 실제라면 카운터가 아니라 입력(프레이밍·조명·거치)을 고쳐야 한다.

주의: 편향 섭동은 각도 수준이라 JVM 경로(현재 엔진)에는 넣지 못했다. 프로토타입의 평활 옵션은 무조건 적용이라(앱은 dt ≤ 350ms 일 때만) 발열 행의 평활 결과는 앱보다 나쁘게 나온다.

### 잡음을 넣어서 "학습" 시키는 안에 대한 판단

잡음 주입은 위처럼 **시험**에는 지금 당장 쓸 수 있고 설계를 바꿨다(발열 내성이 코어 교체의 근거로 추가됐고, 팔 신호의 잡음 적응 평활이 후보로 올라왔다). 학습에 쓰는 것은 지금 단계에서 맞지 않다고 본다.

- **정답의 모양과 양.** 반복 정답이 있는 데이터는 MM-Fit(세트 단위 횟수, 10명, 두 명이 56%)뿐이다. AIHub 는 반복 라벨이 없고, REHAB24-6 은 CC BY-NC 라 측정만 허용되며, 휴대폰 자가 라벨은 한 사용자 몇 세트다. 세트 횟수만으로 시퀀스 모델을 학습시키는 것은 가능은 하지만(횟수 손실), 10명에서 배운 것은 템포·스타일·피험자를 외운다. 잡음 증강은 측정 잡음의 다양성을 늘리지 **동작의 다양성**은 늘리지 않는다.
- **원인이 잡음이 아니었다.** 관측된 실패는 깨끗한 3D 포즈에서 재현됐고 구조적(신호 선택·앵커·체류·평활)이었다. 잡음 강건성은 아직 재지 않은 문제이고, 위 표는 규칙 카운터가 현실적 크기의 잡음에서는 여유가 있음을 보여 준다. 학습으로 풀 문제가 아직 정의되지 않았다.
- **잡음 모델을 모른다.** 폰의 프레임별 σ·튐 빈도·끊김 패턴은 미측정이다. 가정한 잡음으로 학습하면 가정에 맞춰진다. 영상이 오면 MediaPipe↔3D 짝(프레임별)이라는 **실제 잡음**이 생기니, 합성 잡음은 그 전의 스트레스 테스트로 쓰고 학습 입력으로는 쓰지 않는 것이 순서다.
- **원칙과 귀속.** 데이터셋 위에서 상수를 맞추지 않는 원칙(#4, README §5)은 파라미터가 많을수록 더 세게 걸린다. 규칙 카운터는 놓친 반복마다 원인을 붙일 수 있었고(§2) 그것이 이 설계의 근거였다. 학습 카운터의 실패는 귀속되지 않아 "판정하지 않은 것을 판정한 것처럼 말하지 않는다"(#1)를 지키기 어렵다.

학습이 값을 할 자리는 있다. 순서대로:
1. **실제 잡음 측정**(단계 2·4): MediaPipe↔3D 짝에서 관절별 σ·튐·끊김 분포 → 이 문서의 필터 문턱을 실측으로 채운다. 학습 없음.
2. **사이클 게이트 분류기**: 컬 세트 직전 헛카운트(0.12~0.22/세트)가 규칙으로 안 잡히면, 사이클 단위 특징(진폭·길이·복귀 비율·다른 관절의 동반 움직임)으로 "이 종목의 반복인가" 를 가르는 작은 분류기. 양성 = MM-Fit 세트, 음성 = 휴식·타 종목, 잡음 증강 적용, 피험자 홀드아웃. 카운트 자체가 아니라 **거르기**만 맡기고, 규칙 코어는 그대로 둔다.
3. **학습 잡음 제거기**: 짝 데이터에서 구조적 오차(정면 팔꿈치 압축 등)가 보일 때만. 카운팅 신호에만 적용하고 ROM 기준 비교값에는 쓰지 않는다.
4. 하지 않는 것: ROM·깊이 기준의 학습, 종단간 카운터 학습을 ship 경로에 두는 것.

## 10. AIHub GT 3D 확인 결과 (2026-09-24, 사용자 PC 실행)

`research/aihub_fitness/aihub_rep_signal_check.py` → `research/external_rep_replay/results/aihub_rep_signal_check.json` (QC 후 클립, 0.6초 표본 — 사이클 극값은 얕게, 스윙은 작게 잡힌다).

| 종목 · 신호 | 클립 | 사이클 스윙 p10 / p50 / p90 | 게이트 이상 사이클 | 정상 클립 바닥 p90 (ROM 후보) |
|---|---:|---|---:|---:|
| 스쿼트 `knee_mean` | 706 | 61.7° / 88.5° / 115.5° | 0.99 (35°) | 97.9° (현재 값과 같음) |
| 런지 `knee_out_mean` | 1,615 | 0.018 / 0.047 / 0.119 | **0.15** (0.10) | −0.0076 |
| 런지 `knee_mean` | 1,615 | 25.0° / 54.0° / 83.2° | 0.76 (35°) | 146.6° |
| 런지 `knee_minside` | 1,615 | 38.3° / 65.0° / 89.2° | 0.93 (35°) | 125.6° |
| 컬 `elbow_mean` | 1,406 | 68.9° / 97.6° / 120.8° | 0.99 (35°) | 81.3° |
| 컬 `elbow_L` / `elbow_R` | 1,406 | 69.2° / 98.2° (L), 68.5° / 97.9° (R) | 0.99 (35°) | 81.7° / 83.6° |

읽는 법:
1. **런지 `knee_out_mean` 은 AIHub 에서도 통과한 적이 없는 설정이다.** 사이클 스윙 중앙값 0.047, 게이트 0.10 을 넘는 사이클 15%. MM-Fit(0.052)과 같다. "AIHub 에서 배운 것이 MM-Fit·휴대폰에서 깨졌다" 가 아니라, 성긴 표본의 일관성 설문이 고른 신호에 검증 없이 물리 게이트를 붙인 것이다(README §7 가설 판정과 일치). §4.1 의 교체는 AIHub 근거로도 맞다.
2. **런지 신호는 `knee_mean` 대신 `knee_minside` 가 AIHub 에서 더 강하다**(게이트 이상 0.93 vs 0.76). MM-Fit 에서는 반대였다(0.98 vs 0.77, 복귀 히스테리시스). AIHub 런지는 뒷다리가 덜 굽는 클립이 섞여 평균이 작아지고, MM-Fit 은 양 무릎이 함께 깊이 굽는다. **결정 보류** — 영상 단계(MediaPipe)와 휴대폰 라벨에서 두 신호를 나란히 재고 고른다(R4). 둘 다 35° 게이트는 유지.
3. **런지 ROM 후보는 쓰지 않는다.** `knee_mean` 바닥 p90 146.6° 는 사실상 "조금만 굽혀도 유효" 라 기준 구실을 못 한다(0.6초 표본이 바닥을 놓치는 효과 포함). §4.5 대로 런지 ROM 은 없음(항상 유효)으로 둔다. `knee_minside` 125.6° 도 같은 이유로 보류.
4. **AIHub 덤벨 컬은 양팔 동시다**(좌우 상관 중앙값 0.994, MM-Fit 교대 컬 −0.28). ROM 81.3° 는 동시 컬의 평균값에서 나온 기준이고, 팔별 기준도 81.7° / 83.6° 로 거의 같다. **§4.5 의 "교대는 쪽별 판정" 은 기준값을 바꾸지 않고 그대로 성립한다**(팔별 기준 ≈ 평균 기준).
5. **컬 게이트 50° 의 근거가 섰다.** 팔별 정상 반복 스윙 p10 이 약 68° 다. MediaPipe 가 깊은 굴곡을 얕게 읽는 편향(+10~30°)을 빼도 38~58° 이므로 35° 는 넉넉하고, 50° 는 편향이 큰 쪽(+18° 초과)에서 하위 10% 반복을 놓치기 시작한다. **50° 를 기본 후보로, 영상 단계에서 MediaPipe 팔별 스윙 p10 이 50° + 여유를 넘는지 확인한 뒤 확정**한다(§4.4 문구 유지).
6. **스쿼트 35° 게이트는 여유가 크다**(스윙 p10 61.7°, 99% 통과). ROM 97.9° 는 현재 값과 같아 재산출이 재현됐다.

## 11. 휴대폰 검증을 대신할 자료 — 조사 결과와 판단 (2026-09-24)

근거: `research/external_rep_replay/results/rep_datasets_survey.md`(사용자 PC 조사). RepCount 수치는 공개 주석 CSV 로 이 세션에서 다시 확인했다.

| 자료 | 판단 | 쓰임과 이유 |
|---|---|---|
| **MM-Fit 영상** (CC BY 4.0, 21파일 39.1GB) | **채택 · 최우선** | 세 종목(스쿼트·런지·컬)을 모두 가진 유일한 영상. 3D 포즈 재생(§5)과 같은 185세트를 MediaPipe 로 돌려 세트별로 짝지으면 MediaPipe 가 더하는 몫이 나온다. PC 여유 공간(55GB)을 생각해 한 워크아웃씩 받고 추출 뒤 지운다 |
| **REHAB24-6** (CC BY-NC, 측정 전용) | 채택 · 두 번째 | 스쿼트·런지의 반복별 경계 → 경계 F1. 두 카메라는 같은 자리(가로/세로)라 각도 비교는 안 된다 |
| **RepCount 학교 촬영분** (`stu*`: 스쿼트 105 · 프런트 레이즈 97, 학술 연구 한정) | **조건부 — 라이선스 결정 필요** | 반복 수 중앙값 13, 104/105 에 반복별 경계 → "어느 반복을 놓쳤나" 를 사람·장소가 다른 영상에서 잴 수 있다. 측정만, 어떤 상수도 맞추지 않는다. 다운로드는 9.7GB 단일 파일(미러가 조용히 잘리므로 크기 대조 필수) |
| RepCount 유튜브분 (스쿼트 30, 반복 중앙값 2) | 제외 | 세트가 너무 짧다 |
| Countix · OVR | 제외 | 영상이 없고(YouTube ID 만) 클립당 2~3회 |
| UCFRep | 보류 | 스쿼트 21개를 위해 6.5GB |
| InfiniteRep · Fit3D · FLAG3D · FLEX | 제외 | 배포 소실(RGB·라벨 없음) 또는 상업 불가·신청 필요 |
| **AIHub 원본 이미지 → 폰 재생 앱** | 채택 — **폰 USB 연결만 필요** | 폰 MediaPipe(GPU)의 방향별·굴곡 깊이별 관절각 오차. 운동할 사람은 필요 없다. 이번에는 폰 미연결로 못 했다 |

**대신할 수 있는 것과 없는 것.**
- 대신할 수 있다: 연속 동작의 카운팅 논리(완료), MediaPipe 가 영상에서 더하는 오차(MM-Fit 영상 · REHAB · RepCount), 폰 MediaPipe 의 방향별 오차(AIHub 이미지 폰 재생).
- 외부 자료로 못 채운다: **런지·컬의 다양성**(반복 정답 영상이 MM-Fit·REHAB 밖에 없고 둘 다 한 장소·정면), **폰 전용 조건**(IMU·발열·실시간 간격·세트 시작 전후 사람의 행동). 세트 경계 헛카운트(§1 의 가장 큰 위험)는 실제 사람이 폰 앞에서 하는 행동이라 어떤 데이터셋에도 없다.
- 그래서 최종 게이트(폰 자가 라벨)는 없어지지 않는다. 다만 외부 자료로 논리·추정기 문제를 먼저 걸러 두면, 폰 수집은 "세트 경계와 폰 조건" 만 확인하는 작은 규모로 줄어든다.

조사 문서에서 바로잡을 두 곳: ① "지금 쓰는 w06 두 종목" → 지금까지 쓴 것은 **21개 워크아웃 전체의 3D 포즈**이고 빠진 것은 영상(MediaPipe 경로)이다. ② "마지막 렙 구조적 누락" 은 옛 반전형 카운터의 결함이고 §42 이후 라이브 카운터(복귀형)는 해당하지 않는다. Countix 제외 판단은 다른 이유(영상 없음, 클립당 2~3회)로 그대로다.
