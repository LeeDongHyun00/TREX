# 직접 조작·기록 그래프 복구 — §44

2026-09-11. 사용자의 §43 후속 수정 요청을 반영한다.

- 기록의 최근 7일 완료 세트 막대 그래프를 복구한다. 실제 저장된 항목 수를 세며, 0건인 날은 바닥 선과 0을 표시한다. 평균 자세 점수는 만들지 않는다. 날짜별 기록은 처음에는 닫혀 있고 막대 선택 시 해당 날짜를 펼쳐 이동한다.
- 운동 행은 이름/목표/휴식을 왼쪽에, 토글과 수정 버튼을 오른쪽의 같은 행에 놓는다. 토글 아래 `자세 교정` 이름을 항상 표시하고 미지원 항목은 `미지원`으로 표시한다. 긴 이름은 왼쪽의 가변 폭 영역에서 줄바꿈된다. 토글에는 운동명·사용 상태 접근성 정보를 준다.
- 종목 선택의 접힌 카테고리 목록을 상단 가로 탭으로 교체한다. 교체는 추천, 추가는 전체 탭으로 시작한다. 카테고리를 누르면 해당 운동만 표시하고 검색은 전체 카탈로그를 대상으로 한다. 추천의 부위 이름과 종목 행의 목표 횟수는 빼고 운동 이름만 보여 준다. 실제 추천 후보를 고르는 기존 부위 로직은 유지한다.
- 자세 교정 라이브의 더 보기 메뉴와 그 안의 모드/상세 안내 진입을 제거한다. 하단에 종료(왼쪽)·음성·카메라·건너뛰기(오른쪽)를 직접 배치한다. 빈도가 높은 일시정지/재개와 횟수 조작은 그 아래 오른쪽 주요 버튼을 유지한다. 음성은 현재 켜짐/꺼짐을 표시한다. 카메라 전환은 기존 개인 관측 기준·진행 중 반복 사이클을 재설정한다. 카메라를 사용하지 않는 타이머 화면의 기존 메뉴는 이번 요청 범위 밖이다.
- 로그인은 §42 이전의 로고 카드·로그인/회원가입 탭·입력창·버튼 배치로 복구한다. 아이디/비밀번호 찾기는 유지하고 가이드북·실험실·기준선 설정의 로그인 진입 링크는 제거한다. 최초 실행 가이드 자체는 유지한다.
- 홈 운동하기 카드는 운동 탭으로 이동하며 세션을 바로 시작하지 않는다. 오늘 섭취 카드와 식사 기록 카드 전체 영역은 식단 탭으로 이동한다.

자세 규칙/신뢰 등급/카운터 알고리즘을 바꾸는 작업은 아니다. 저장된 COACH/TRACK 선택값과 판정 정책도 유지한다. UI 테스트는 자세 정확도나 새 사용자에서의 일반화 검증을 뜻하지 않는다.

## 검증

단위 검사 **226건 통과**, Galaxy Note10+ 분리 앱 실기기 검사 **10건 전체 통과**(102.571초). 토글과 수정 아이콘의 화면 중앙 좌표 차이가 4px 이내인지 검사했다. 추천/카테고리 선택, 그래프→날짜 이동, 음성/카메라/건너뛰기/종료와 취소, 로그인 링크 제거, 홈 이동 및 기존 운동 진행을 확인했다. 실제 캡처로 목록·그래프·종목 탭·라이브 조작부·로그인 배치를 검토했다.

일반 앱과 검증 앱의 DEX/자세 자산 12개가 동일하다. APK·캡처·검사 로그는 `research/aihub_fitness/outputs/direct_ux/`에 보관한다. 이 검증은 Note10+의 화면/저장 동작 검사이며 새 자세 정확도 측정은 아니다.


## 설치 확인

2026-09-11 17:24(KST), Galaxy Note10+ 일반 앱을 삭제 없이 `install -r`로 업데이트하고 실행했다. 설치된 APK와 로컬 APK 해시가 일치한다.

- 파일: `research/aihub_fitness/outputs/direct_ux/trex-direct-ux-debug.apk`
- SHA-256: `5c9a8539e83abac89e9877040e4696b454c70397919936619eee22b6394c4112`
- 설치 직전 `device_preinstall/`에 환경설정을 백업했다. 설치 후·실행 후 `trex_store.xml` 13,625바이트, `trex_posture.xml` 301바이트 및 WebView 설정이 모두 백업과 바이트 단위로 같다. 기존 기록·루틴·자세 설정을 보존했다.
- 최종 증거: `outputs/direct_ux/verification.json`, `device-tests.log`, `package_parity.json`, `screenshots/`.
