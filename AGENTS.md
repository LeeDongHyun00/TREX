# TREX Agent Instructions

## 기능 구현 Git 전달 규칙

- 코드·리소스·빌드 설정 등 사용자 기능을 구현하거나 수정하는 작업에는 이 규칙을 항상 적용한다.
- 작업 시작 시 현재 브랜치를 확인한다. `main`, `master` 또는 원격 기본 브랜치에 있다면 구현 전에 `codex/<short-description>` 형식의 기능 브랜치를 만든다.
- 이미 해당 작업에 적합한 기본 브랜치가 아닌 브랜치에 있다면 새 브랜치를 중복 생성하지 않고 현재 브랜치를 사용한다.
- 구현 후 관련 테스트·빌드·lint를 실행하고, 이번 작업 범위의 파일만 명시적으로 stage한다. 사용자의 무관한 변경은 stage하거나 되돌리지 않는다.
- 검증이 통과하면 현재 기능을 설명하는 간결한 메시지로 commit하고 `git push -u origin <current-branch>`로 push한다.
- force push, 기록 재작성, 파괴적 reset은 사용자가 명시적으로 요청하지 않는 한 금지한다.
- 인증·네트워크·원격 거부로 push할 수 없으면 로컬 commit은 보존하고 정확한 실패 원인과 사용자가 해야 할 조치를 보고한다.
- 읽기 전용 분석, 설명, 코드 리뷰처럼 기능 변경이 전혀 없는 작업에는 branch·commit·push를 강제하지 않는다.
