#!/bin/bash
# 선택한 묶음(11 04 06_07_08 09_10)을 순서대로 처리한다. 동시 다운로드는 하지 않는다.
# PC 재부팅·절전 후에도 이 스크립트를 다시 실행하면 .done 이 없는 묶음부터 이어간다.
set -u
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
bash "$ROOT/tools/pipeline.sh" "$1" 11 04 06_07_08 09_10
